# Fixture — excluded workflow names

O gate do template roda em `release`, também escrito `release.yml` ou `release.yaml`.

Nada disso é workflow excluído: `pnpm catalog:lint`, `catalog/`, `ci.yml` e `releases`.
