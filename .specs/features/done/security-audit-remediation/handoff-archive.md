# security-audit-remediation — Handoff archive

Moved out of `.specs/STATE.md` § Handoff at closeout (2026-08-23). Final state: Verifier PASS on round 3 (`validation.md`), 69 tasks / 15 clusters / 8 waves, branch `feat/security-audit-remediation` at `efb6ba9` (worktree `.worktrees/security-audit-remediation`) merged into `main` at `2f84f9f` and released as kernel `v2.1.0` (`2bd4da3`; `v2.0.0` had been tagged earlier without it).

- Feature `security-audit-remediation` — **Specified + Designed + Tasked (2026-08-22); Execute started 2026-08-22 in `.worktrees/security-audit-remediation` on `feat/security-audit-remediation`.** `tasks.md` holds **58 tasks / 11 clusters / 5 waves** (W1 kernel ×3 ∥; W2 entries + kernel consumers ×5, four dispatched and C8 as the FIFO tail; W3 exclusive lockfile; W4 exclusive contract regen; W5 release plumbing) with the four pre-approval checks all ✅. Two cross-cluster edges inside W2 were removed at authoring: identity `domain/errors.ts` additions moved to W1 (T7, removal of `EmailBelongsToDeletedUserError` stays with its only producer T37) and kernel `redaction/sensitive-keys.ts` moved to W1 (T12) so C4 and C8 only consume it. Every entry `module.json`/`CHANGELOG.md` is owned by W5 alone, so no entry cluster touches a manifest. **Kernel tag decided by the user on 2026-08-22: `v2.0.0`** (all five entries bump `kernelRange` to `">=2.0.0 <3.0.0"`). Three Test Coverage Matrix rows were added for layers with no importable surface (entry boot seed, custom SQL migration, import-path rewrite) so no task carries an unjustified `Tests: none`. Verifier at **opus** (auth + data integrity, P0). **Pre-flight DONE (2026-08-22)**: specs committed on `main` at `77d2a05`; worktree `.worktrees/security-audit-remediation` on branch `feat/security-audit-remediation`; baseline **51 suites / 330 tests** (the pre-feature count the Verifier compares against). **Wave 1 DONE (2026-08-22), Build gate PASS** — 20 commits `77d2a05..8f9c8ee` on the feature branch (C1 opus T1–T7, C2 sonnet T8–T12, C3 sonnet T13–T18); typecheck 0 · lint 0 · unit 60 suites / 395 tests · `test:scripts` 200 · `catalog:typecheck` 0; per-task hashes, deviations and plan corrections in `tasks.md` § *Execution record* (committed on `main` at `dfb5234`). One blocked-by-ownership so far (T6: 27 identity controllers imported the deleted guard — fixed `b6f91d9`, `Touches` corrected); a second one in the feature triggers the Touches audit over every undispatched cluster. `src/modules/architecture.spec.ts` does not exist — Build gates name only `module-boundaries.spec.ts`. Catalog commits use the trailer `Advisory: none — covered by ADV-20260822-NN (security-audit-remediation)` (01 identity · 02 attachment · 03 notification · 04 audit · 05 tag); the advisory files land in wave 5. **Wave 2 DONE (2026-08-22), Build gate PASS on the 2nd run** — 37 commits `8f9c8ee..16a9645` (C4 opus T19–T23, C5 opus T24–T30, C6 opus T31–T38, C7 sonnet T39–T46, C8 sonnet T47–T50 FIFO tail, gate-fix sonnet ×4 commits); the last Jest gate: typecheck 0 · lint 0 · unit 60 / 424 · boundaries 32/32 · `test:scripts` 200 · `catalog:typecheck` 0 · catalog specs in the stage 114 suites / 850; record, deviations and plan corrections in `tasks.md` § *Execution record* (lockId of `outbox-dead.purge` is **6**, design § C corrected; C6's `Touches` gains `domain/errors.spec.ts`; C7 strayed into the primary checkout once — T39 relocated as `1c1cdfe`, `main` reset — **workers must never run anything in the primary checkout**). No second blocked-by-ownership (the C6 edit was an unowned file, not a stop). Every entry int/e2e proof of waves 1–2 is still **unrun** (Jest stage has no entry migrations, parity specs not staged) — they run first in T61. **`vitest-migration` merged into `main` at `278dde0` during wave 2** (Jest gone; root-only `test*` scripts; `globals: false`; pre-push `migrations → typecheck → catalog-typecheck → test-coverage`); the user added the port to this spec (commit `37dcc70` on `main`): spec story *P1 Port* + REM-48..51, design § H (merge-not-rebase, conflict table, codemod `scripts/platform/jest-to-vitest.mjs`, proof paths: entries only via `pnpm catalog:check`), tasks **C12 = T59→T62 as exclusive wave 3**; old waves 3/4/5 are now 4/5/6 (C9 T51 lockfile · C10 T52 contract regen · C11 T53–T58 release). **Open for the user before wave 6**: version fold — default is entries stay `2.0.0` (already bumped by the Vitest advisories `ADV-20260821-01..05`, unreleased) + `ADV-20260822-NN` beside them + `kernelRange` `>=2.0.0 <3.0.0` + kernel tag `v2.0.0` covering both; alternative = bump again (identity 3.0.0, attachment/notification 2.1.0, audit/tag 2.0.1). **Wave 3 DONE (2026-08-22), Build gate PASS on the 1st run** — C12 opus: T59 `aeedbfd` (merge of `main@2bdee72`, 25 conflicts) · T60 `f6e23a4` · T61 `bd8a4e9` · T62 no-op; the first Vitest gate: `pnpm check` 0 · `pnpm test` 86 files / 546 · boundaries 32/32 · `catalog:typecheck` 0 · `test:int` 119 · `test:e2e` 14 · **`catalog:check` exit 0 (child 1499 unit + 498 int/e2e; `contract.parity.spec.ts` is green — no red to carry to T52)**. The wave-2 unrun entry proofs ran for the first time and exposed 5 real defects, fixed at source (notably a busboy off-by-one in attachment `multipart-files.ts` — `parts: maxFiles` rejected a single legal file); record and deviations in `tasks.md` § *Execution record*. **Open: `pnpm test:coverage` is red on the merged tree by inheritance from `main`** (branches 88.54 % global / 87.56 % `apps/api/src/**` vs flat 90; offenders `client.factory.ts`, `dedicated-client.ts`, `event-context.ts` — untouched by the feature; floors not lowered) — `main`'s `api-coverage-to-90` closes it; **merge `main` once more before the Verifier** and re-run `pnpm test:coverage`. New Verifier baseline: 86 / 546 (post-merge). **Wave 4 DONE (2026-08-22) + gate-fix (2026-08-23), wave 5 DONE (2026-08-23), both Build gates PASS** — C9 sonnet T51 `298e8f3` (multer 2.2.0, root `pnpm.overrides`, 21 → 2 highs; the residue is the Out-of-Scope `api-client>axios` chain, so `pnpm audit` exits 1 by design) + gate-fix `6c8e35e` (wave-4 gate skipped `catalog:check`; T51's `fields: 0` broke the access-link avatar `token` field → `fields: 1`, **SPEC_DEVIATION vs REM-39 for the Verifier**; spec lint); C10 opus T52 `65af323` (`openapi.json` 1-line delta, client byte-identical, parity needed no edit — child 14 parity files / 89 tests green). Gate: `pnpm check` 0 · `pnpm test` 86 / 546 · boundaries in run · `catalog:typecheck` 0 · `catalog:check` 0 (216 unit + 498 test:db). Plan correction: any wave touching `catalog/**` code includes `pnpm catalog:check` in its Build gate. **Wave 6 DONE (2026-08-23), Build gate PASS on the 1st run** — C11 sonnet: T53 `f1e76ca` · T54 `d975c1e` · T55 `e8d7195` · T56 `b9b0ca1` · T57 `5bb31ff` · T58 `a4ebe34` (range `6c8e35e..a4ebe34`); version fold applied by default (entries stay `2.0.0`, items in the existing `## [2.0.0]` sections, `ADV-20260822-01..05` beside the Vitest advisories, `kernelRange` `>=2.0.0 <3.0.0` ×5, kernel tag `v2.0.0` covers Vitest + security — the maintainer applies `git tag v2.0.0` after merge); `template-changelog.md` carries `lockId` 6 and the `pnpm contract` kernel-env DX note; the worker died once mid-T53 (host sleep) and was resumed from its transcript. Gate: `pnpm check` 0 · `pnpm test` 86 / 546 · boundaries 32/32 · `catalog:typecheck` 0 · `catalog:lint` 0 · `template:smoke` 4/4 · `catalog:check` 0 (child 216 unit / 1500 + 70 test:db / 498). Record in `tasks.md` § *Execution record*. **All 58 tasks DONE; pre-Verifier merge `ff8c6db` (main@8a06441, clean) — `pnpm test:coverage` green on the merged tree (103 files / 722 tests, branches 94.25 %, main's `api-coverage-to-90` landed). Verifier round 1 (opus, 2026-08-23): FAIL** — `validation.md` written in the worktree: 45/51 ACs matched, Final gate 10/11 (only `pnpm audit` non-zero, residue = the Out-of-Scope `api-client>axios` chain), sensor 4/5 (mutant 2 survived at attachment `upload-attachments-batch.use-case.ts:67` — the `sniffed !== file.contentType` branch is undiscriminated). Gaps → fix tasks **T63–T68** in `tasks.md` (after T62): REM-08 mutant (T63), REM-14 quotas 429/413/503 (T64), REM-12 post-header stream failure (T65), REM-15 `fieldSize` + `parts` (T66), REM-40 three hash columns in the trail (T67), REM-28 seed unit spec (T68). Spec-precision fixes applied by the orchestrator in `spec.md` (REM-39 filtered audit gate; REM-48 probe grep tightened; Success Criteria #2 restated). Cosmetic narrower proofs (REM-09/10/19/25/27) accepted. **Wave 7 (fix round 1) DONE (2026-08-23), Build gate PASS on the 1st run** — C13 sonnet T63 `b3b1259` · T64 `1937907` · T65 `b721676` · T66 `ca6bb6f` (mutant 2 kill verified by re-injection; multipart parser is at `api/api/controllers/multipart-files.ts`) ∥ C14 sonnet T67 `63f1e5e` (hosted in the audit precedent `audit-trigger.int-spec.ts` — an identity-only child has no `audit.entries`; **SPEC_DEVIATION**: the spec re-attaches the three tables with migration 03's redact lists since the shared helper mirrors migration 02) · T68 `1ec6034`. Gate: `pnpm check` 0 · `pnpm test` 89 / 585 · boundaries 32/32 · `catalog:typecheck` 0 · `catalog:lint` 0 · `catalog:check` 0 (child 221 / 1548 + 70 / 510). Range `3b7554f..ca6bb6f`. **Verifier round 2 (2026-08-23) — FAIL, 1 Major**: 50/51 ACs, Final gate 12/12, sensor cumulative 6/8 — **mutant 7 survived: REM-40 is not closed in product** (`03_audit_redact_token_hashes.sql` is guarded on `audit.attach` existing, identity installs before audit, so it never runs in a stock child; the T67 int-spec only proved the trigger via an inline re-attach). Fix task **T69** authored in `tasks.md` after T68 (wave 8 = C15, **opus**, exclusive — migration + install order, cross-entry; preferred design (a): an audit custom migration guarded on `to_regclass('identity.sessions')` that attaches identity's tables after identity, installer hook (b) only if `scripts/platform` already has one; the inline re-attach and the `reattachIdentityTables` helper go; check migration 02 (`users.password_hash`) has the same hole). REM-48 probe re-tightened (`jq -r '.scripts | keys[]'`). Git note: the wave-7 `STATE.md` commit landed on the feature branch as `89681d8` and was cherry-picked to `main` as `17665a7` (same content — the next merge is clean). `validation.md` round 2 + lessons committed on the branch at `a97bb01`; `main@4f7b575` merged into the worktree at `ee64726` (STATE.md conflict resolved by taking `main`'s) — worktree HEAD `ee64726`, clean. **Wave 8 (fix round 2) DONE (2026-08-23), Build gate PASS on the 1st run** — C15 opus T69 `621c5da`: the identity trail never attached in a fresh child (audit installs last, identity's `02`/`03` guards were dead code — `users.password_hash` too); now identity `04_audit_attach_hook.sql` declares `identity.attach_audit()` (14 tables, 4 redacted columns, self-`PERFORM` if audit is present) and audit `migrations/custom/02_attach_module_hooks.sql` runs every installed `<schema>.attach_audit()` at the end of audit's install; inline re-attach gone, `reattachIdentityTables` reduced to the shipped statement; mutant 7 killed by name. Gate: `pnpm check` 0 · `pnpm test` 89 / 585 · boundaries 32/32 · `catalog:typecheck` 0 · `catalog:lint` 0 · `test:scripts` 233 · `catalog:check` 0 (child 221 / 1548 + 70 / 510). **Next: resume the same Verifier (round 3 of 3) with the fix range `ee64726..621c5da`** — REM-40 + the migration redesign (design § D needs the hook noted at closeout), re-run mutant 7, Final gate; if it fails again, escalate to the user. After PASS: closeout (move to `done/`, archive this entry), then the user merges `feat/security-audit-remediation` and tags. Historical context below. Closes every High + Medium of the 2026-08-22 backend security audit plus the one-to-two-line Lows; structural Lows and Infos deferred to `security-hardening-2` (spec Out of Scope). Artifacts: `.specs/features/security-audit-remediation/{spec.md,context.md,spike.md,design.md,touches-audit.md}` — `spike.md` holds per-finding `file:line` + repro (the Verifier anchors each AC there), `context.md` the four Specify decisions, `design.md` (~34 kB, Large) the architecture in seven areas A–G, `touches-audit.md` the per-vertical file ownership lists for tasks.md § 3 (all five sections complete: attachment, rate-limit/login, kernel config/outbox/build/CI, identity authz + tag/audit/notification, test commands — incl. the gate constraint that entry int/e2e only run inside a staged child, and that REM-31 needs a new `assertPermission` helper). 5 stories / 47 requirements (REM-01..47). Design decisions: kernel rate-limit seam (`shared/kernel/rate-limit/` port + `InMemoryRateLimiter` + `ResilientRateLimiter` with per-call `critical`, decorator + guard class; identity registers `APP_GUARD` as `[CsrfGuard, RateLimitGuard]`; outage signalled via `EventEmitter2`), `BoundedPasswordHasher` over a kernel `InFlightGate`, `BreachCheck` verdict port (`clear|breached|skipped`, `fail_closed` → 503), kernel `redaction/sensitive-keys.ts` shared by log redactor + outbox + notification, `outbox-dead.purge` (`lockId` 3), attachment `openStream()` thunk + `pipeline` + inline allowlist + busboy `limits` + stream sniff + owner quota + `UploadGate`, S3 `requestHandler` timeouts (verified `NodeHttpHandlerOptions`), `env.ts` fail-closed + `/docs` gate, swc `ignore` for harness dirs, boot seed `api/seeds/bootstrap.ts` + entrypoint glob, catalog-lint rule on `testing/**` imports. AD-029/AD-030/AD-031 appended as `planned`. Versions: identity 2.0.0 (breaking), attachment 1.1.0, notification 1.1.0, audit 1.0.1, tag 1.0.1; kernel tag proposed **v2.0.0** (user may choose v1.3.0). Proofs: 44 `test`, 1 `gate`, 2 `probe`. Next: **Tasks** — read `design.md` § *Execute notes* (wave sketch: W1 kernel ×3 clusters ∥, W2 entries ×4 ∥, W3 deps/lockfile exclusive, W4 contract regen + advisories/docs exclusive; Verifier at opus) and `touches-audit.md`, generate the Test Coverage Matrix from `docs/test/testing.md` + jest configs, author `tasks.md` (~40 tasks, cap ~40 kB), run the four pre-approval checks; the `wave-plan-check` hook re-checks sibling overlap on every write. **Do not dispatch Execute without the user's explicit ok.** Advisory commit protocol: first commit per entry stages `docs/advisories/ADV-<date>-NN.md`; later commits in the same entry use the trailer `Advisory: none — covered by ADV-<date>-NN (security-audit-remediation)`. Independent of `vitest-migration`/`test-suite-refactor`; if those land first, tests here are authored in Vitest. Pre-condition for Execute: the owner commits or branches the unrelated uncommitted tooling on `main` before `git worktree add .worktrees/security-audit-remediation -b feat/security-audit-remediation main`.
