# Pacotes @bryntum vem do registry privado (licenca comprada), nao do npm publico.
# O token nao mora aqui: este arquivo e versionado.
#   local  -> ~/.npmrc com //npm.bryntum.com/:_authToken=<token>
#   CI/Docker -> mesma linha injetada via secret
@bryntum:registry=https://npm.bryntum.com
