# Project State — platform-template

## Decisions

Active project-level decisions. Every design must conform or supersede explicitly.

| ID | Status | Decision |
| --- | --- | --- |
| AD-001 | superseded by AD-013 (v1) | **Product slot files.** Contract-facing closed sets (permission catalogs, access profiles, upload profiles) are extended through *static slot files* the platform ships empty and the product fills with additive one-liners: `shared/kernel/access/product-permission-catalogs.ts` (precedent), `shared/kernel/access/product-access-profiles.ts`, `shared/kernel/upload/product-upload-profiles.ts`. They are static because the Zod contract (`z.enum`) is evaluated at import time and must reach `openapi.json` → Kubb → web with literal types. Slot files are the only platform files a product edits, alongside the composition root (`app.module.ts`, `db/schema.ts`, `product-routes.tsx`); the platform never adds content to them after creation, so `copier update` 3-way merge stays clean. Behaviour that is only needed at boot (notification template sources, audit registrations) uses **runtime registries** the product fills from its own module (`OnModuleInit`), never a slot file. |
| AD-002 | superseded by AD-014 (v1) | **`professional` stays a base-set access profile in v0.2** (`master`, `admin`, `professional`). The identity professional slice (scope/commitments ports, directory + assignment facades, five satellite tables in baseline 0000, `areaIds/serviceIds/schedulingAreaIds` in the user contract) is far larger than the profile literal; extracting it is a v0.3-size change with its own migration story. The profile registry (AD-001) makes that extraction a deletion later. Pending user confirmation — see spec Assumptions. |
| AD-003 | superseded by AD-014 (v1) | **`attendsGuests` → `servesClients`** (column `serves_clients`). Neutral across service businesses; keeps the verb-phrase boolean style already used by the field. `isServiceProvider` rejected: `service` is product vocabulary in the coverage sweep. |
| AD-004 | superseded by AD-014 (v1) | **Access profile persistence stays a DB enum** (`identity.access_profile`); a product adds a value with `ALTER TYPE identity.access_profile ADD VALUE IF NOT EXISTS '<key>'` in its own migration. Trade-off table in design.md. Caveat documented for products: a value added inside a migration transaction cannot be used by DML in the same migration batch (Postgres ≥ 12). |
| AD-005 | superseded by AD-015 (v1) | **Platform migrations after the baseline exist.** They keep `NNNN_<module>_<slug>` numbering continuing from 0004 and a `when` chosen at authoring time. Products are told to number their migrations from `1000_` (idx collisions are tolerated by drizzle, order = journal position) and, after `copier update`, to bump the `when` of incoming platform entries above their last applied migration when `db:check:journal` fails (a not-yet-applied entry may be re-stamped; an entry born in the past is silently skipped forever). |
| AD-006 | active (amended by AD-016) | **Version truth = git tag + `docs/dev/template-changelog.md`.** `package.json` versions are not bumped (would churn every child's manifests). Every tag carries a migration note for children in the changelog. |
| AD-007 | superseded by AD-014 (v1) | **Notification template source shape.** A registered source is `{ type, catalog, email? }` where `email = { template, templateDir?, subject(data), recipient?(data), view?(data) }`. `templateDir` absent → the notification module's own `templates` dir; `recipient` absent → `data.email` (must be a string); `view` absent → identity. Base-set types are registered through the same registry, so `EmailChannel` and the renderer resolve every type the same way and hold no per-type code. Breaking for v0.1 products that registered `{ template, templateDir, subject }` at the top level — migration note. |
| AD-008 | superseded by AD-014 (v1) | **`Mailer` is transport-only**: `send({ to, subject, html, idempotencyKey? })`. Rendering happens in `EmailChannel` (binding → recipient → subject → template → transport). Test doubles implement one method. `LogMailer` logs `to`, `subject`, `idempotencyKey` and the `href`s extracted from the HTML so dev flows keep the raw link in the log. |
| AD-009 | superseded by AD-014 (v1) | **Audit extension is one runtime registry** (`AuditRegistry`, exported by `AuditModule`): ref targets (column → `{schema, table, labelColumn}`), table owners (table → permission key), aggregate satellites and technical tables. Base-set (identity, tag) registrations move behind the same registry so consumers have one lookup path. Duplicate registration throws. Coverage lists (`AUDITED`/`EXEMPT`/`MODULE_SCHEMAS`) stay a platform-only test guard. |
| AD-010 | superseded by AD-014 (v1) | **Generic upload profiles**: `avatar`, `access-link-avatar`, `document`, `image`, `multi`. `feedback-attachment` becomes `multi` (route-uploadable, any bytes, ≤100 files, restricted); `credit-receipt`, `accommodation-type-image`, `report-artifact` are removed. Env `ATTACHMENT_FEEDBACK_MAX_FILE_BYTES/TOTAL_BYTES` → `ATTACHMENT_MULTI_MAX_FILE_BYTES/TOTAL_BYTES`; `ATTACHMENT_REPORT_MAX_BYTES` dropped. Product profiles carry static limits in the slot file. |
| AD-011 | superseded by AD-013 (v1) | **Coverage sweep verdicts are the deliverable, not the fixes.** Hits inside the five points' files are fixed in v0.2; every other `remove` / `open slot` verdict becomes a follow-up GitHub issue linked from the verdict table posted on issue #1. |
| AD-012 | superseded by AD-027 (2026-08-22) | **Pre-push coverage bar is 95% S/B/F/L** on api unit (`jest --coverage`, `coverageProvider: "v8"`, `@swc/jest`) and web (`vitest --coverage`, v8). Combined `test:cov:all` nyc floors stay independent and are not this bar. Future features must not lower the pre-push thresholds. |
| AD-013 | active (v1, 2026-08-21) | **Catalog model — supersedes AD-001, AD-011.** The template ships the kernel only; former base-set modules are entries in `catalog/` (in this repo, `copier.yml` `_exclude`), copied into and owned by the child via `pnpm platform module add <name> [--variant]`, recorded in `.platform-modules.lock`. Slot files and the base-set notion are retired; `module-boundaries.spec.ts` gains RULE C (no module vocabulary in `shared/**`, web `app/**`/`shared/**`). One entry per module, variant = sub-entry, no bundles (`dependsOn` composes). Sweep follow-ups #2–#8 are absorbed by entries (`module.json.absorbs`). |
| AD-014 | active (v1, 2026-08-21) | **Module internals are entry-local decisions — supersedes AD-002, AD-003, AD-004, AD-007, AD-008, AD-009, AD-010.** Profiles/professional slice, column names, DB enum policy, notification source shape, mailer, audit registry, upload profiles live in the entry README § Decisões and CHANGELOG; STATE.md governs kernel + tooling only. |
| AD-015 | active (v1, 2026-08-21) | **Migrations — supersedes AD-005.** Template journal = `0000_kernel_baseline` + `0001_kernel_outbox_notify`, kernel numbering `NNNN_kernel_<slug>`. Entries ship tables as TS + `migrations/custom/*.sql`; `module add` generates the migration in the child with `drizzle-kit generate` (+ `--custom` for shipped SQL), so numbering, `when` and the snapshot chain are the child's. Products keep `1000_`. Existing v0.2 children are never rewritten (`module adopt`). |
| AD-016 | active (v1, 2026-08-21) | **Entry versions — amends AD-006.** `module.json.version` + CHANGELOG heading + tag `catalog/<name>[-<variant>]@x.y.z` are the truth for an entry; advisories `affects` target entry versions. Template tag + `docs/dev/template-changelog.md` remain the kernel truth; `package.json` still not bumped. |
| AD-017 | active (v1, 2026-08-21) | **Kernel access seam.** Kernel ships `@RequirePermission`/`@Public`/`@Authenticated` metadata and a global `AccessGuard` that delegates to an `ACCESS_POLICY` port; no provider → 403 `access-policy-missing` on non-public routes (fail closed; default requirement = authenticated). ALS holds an opaque `Actor { id, kind, tenantId? }` + symbol-keyed `extensions`; identity sets the actor from middleware and implements the policy. `tenantId` is the only multi-tenant seam. |
| AD-018 | active (v1, 2026-08-21) | **Raw web for entries.** An entry's `web/core` is pure TS (`zod`, `@platform/api-client` only); `web/react` only react-query hooks/options; no components, pages, routers (children run Vite or Next). UI/router wiring ships as README recipes. Generated client never shipped; `module add` runs `pnpm contract`. |
| AD-019 | active (v1, 2026-08-21) | **Advisories channel.** `docs/advisories/ADV-YYYYMMDD-NN.md` (frontmatter `id, kind, module, affects, severity, detect, fix, parity`), immutable, reach children via copier; child keeps `docs/advisories/APPLIED.md` (`_skip_if_exists`); `.claude/hooks/pending-advisories.mjs` injects `advisories − ledger` filtered by lock versions. Lefthook `commit-msg` rejects a catalog entry code change without an advisory for that module (or trailer `Advisory: none — <reason>`). |
| AD-020 | active (v1, 2026-08-21) | **Generated registries.** `apps/api/src/platform-modules.ts` (`PLATFORM_MODULES`) and `apps/api/src/db/platform-schema.ts` are generated from the lock by `module add/adopt/--rollback`; `app.module.ts` and `db/schema.ts` spread/re-export them. No markers, no hand edits; web has no registry (raw web). |
| AD-021 | active (v1, 2026-08-21) | **Entry-to-entry coupling goes through ports — extends AD-013/AD-017.** An entry must install alone in a kernel-only child. Where one entry needs another's behaviour, the consumer declares a **port** and the provider binds it (the `ACCESS_POLICY` pattern), so `dependsOn` stays acyclic and a missing provider degrades that feature instead of failing module resolution. Discovered in v1 wave 3: `identity` imported `AttachmentModule` unconditionally and injected `AttachmentFacade` in three use cases (`upload-avatar`, `upload-access-link-avatar`, `set-password`), while `attachment` injects identity's `UserDirectoryFacade` — a real `identity ↔ attachment` cycle that `resolveDeps` topo-sorts over. Resolution: identity declares a file-storage port; attachment provides it. Bundles/joint install rejected (AD-013 forbids bundles); tolerating cycles in `resolveDeps` rejected (defeats install ordering). |
| AD-022 | active (v1, 2026-08-21) | **Maintenance jobs are a runtime registry, not a closed kernel union — extends AD-020.** `MAINTENANCE_SCHEDULE` is a hardcoded `const` at `apps/api/src/shared/kernel/scheduling/maintenance-schedule.ts:23-28` (`{ "outbox.purge": {cron,lockId:1}, "idempotency.purge": {cron,lockId:2} } as const satisfies Record<string, MaintenanceJobSpec>`; `MaintenanceJobSpec = { cron: string; lockId: number; atomic?: boolean; timeZone?: string }`), and `@MaintenanceJob(name)` (`maintenance-job.decorator.ts:5,22`) only accepts a name already present as a key — so **no catalog entry can register a maintenance job without editing a kernel file**, and identity's real jobs `auth-events.purge` / `email-change.revert` are dead code in every child (an earlier draft named `purge-users`, which is an admin route, not a job — corrected in wave 4b.2). Resolution: the kernel ships a `MaintenanceRegistry` (same shape as AD-009's `AuditRegistry`); entries register at boot; duplicate name and colliding `lockId` both throw; the kernel's own two jobs register through the same path. Readers to migrate: `maintenance-runtime.ts:14,135,174`, `maintenance-job.decorator.ts:5,22`, `maintenance-schedule.spec.ts:8,17,30,141,145,150`, `maintenance-runtime.int-spec.ts:20,116`. The spec at `maintenance-schedule.spec.ts:138-146` ("varredura resolve exatamente um arquivo de corpo por job do registro") asserts one body file per registered job — it must become registry-driven or be scoped to kernel jobs only. Discovered in v1 wave 4 (T22 cutover); implemented by T22d. |
| AD-023 | planned (test-suite-refactor) | **Test harness layering.** Runner plumbing (the `vitest.*.mts` configs, containers, env) lives in `apps/api/` and `apps/api/test/`; everything a spec imports lives in `apps/api/src/shared/test/{unit,int,e2e,parity,hygiene}` with kernel vocabulary only; each catalog entry ships `api/testing/` (seed, login, fakes, fixtures) listed in `module.json.files`, importable by another entry only along `dependsOn` and only where the graph stays acyclic (**RULE D**, `module-boundaries.spec.ts` + `catalog-lint`, per AD-025). Web mirrors it in `apps/web/src/shared/test/`. Test files may not define local copies of harness helpers — enforced by a committed guard spec (`shared/test/hygiene/harness-hygiene.spec.ts`) that runs in the template and in every child, not by review; lint forbids `.only`/`.skip`/assertion-less/existence-only tests. Constrains v1 entry anatomy (README § Tests) and `docs/test/testing.md`. |
| AD-024 | active (v1, 2026-08-21) | **Entry-to-entry port tokens live in the kernel — closes a hole in AD-021.** AD-021 says the consumer declares a port and the provider binds it, but a token declared inside the consumer entry cannot be bound without the provider importing that entry — the violation AD-021 exists to prevent, in the reverse direction. Wave 4b.2 hit it head on: identity declared `AUDIT_TRAIL_PURGER` in `catalog/identity/single-tenant/api/domain/ports/audit-trail-purger.ts` and the audit entry could not bind it. The `ACCESS_POLICY` precedent works only because its token lives in the kernel. Resolution: the **token and interface** of every entry-to-entry port live in the kernel, in module-agnostic vocabulary (RULE C still applies), beside the concept they belong to (`shared/kernel/<concept>/<name>.port.ts`, the `access/access-policy.port.ts` shape) — **not** in a separate `ports/` tree, which T22i found would be a second lookup path for the same kind of thing (AD-009). Landed at `shared/kernel/audit-trail/audit-trail-purger.port.ts` and `shared/kernel/profile-image/profile-image-store.port.ts`; the consumer injects with `@Optional()`, the provider binds, neither imports the other. Applies to `AUDIT_TRAIL_PURGER`/`AuditTrailPurger` and to T17c's `PROFILE_IMAGE_STORE`/`ProfileImageStore`. Implemented by T22i. |
| AD-025 | active (v1, 2026-08-21) | **Port inversion is required only where a cycle exists; elsewhere `dependsOn` carries the edge — narrows AD-021.** AD-021 reads as "an entry must install alone in a kernel-only child", which taken literally would forbid every entry-to-entry edge. Wave 4c.2 measured the real graph and that reading is unaffordable and wrong: 18 production-source cross-entry imports remain (`identity → notification` ×10 `NotificationRequested`; `audit → identity` ×7 incl. `audit.module.ts:3` importing `IdentityModule`; `attachment → identity` ×1 `UserDirectoryFacade`), and **the production graph is acyclic** — topological order `notification, identity, audit, attachment, tag`. Inverting all 18 would require kernel ports for `UserDirectoryFacade`, `PermissionCatalog` and `NotificationRequested`, i.e. pushing module vocabulary into the kernel, exactly what RULE C forbids. `dependsOn` is already a DAG that `resolveDeps` topo-sorts, and `attachment`/`audit` already declare `identity`. Resolution: **invert only edges that close a cycle**; declare the rest honestly in `dependsOn`. The one cycle today is `identity ↔ notification`, closed solely by 6 test-only imports of identity's `RATE_LIMITER` from notification's and tag's e2e — those are what must be inverted (T22m). AD-021's port mechanism is unchanged for cycles; only its "install alone" absolutism is narrowed. |
| AD-026 | active (v1, 2026-08-21) | **A cross-entry e2e lives in the entry downstream in the `dependsOn` DAG — in the dependent, never in the dependency.** Corollary of AD-025, found by T22m. Four of notification's e2e (`notifications-{email,feed,inapp,sse}`) drove identity routes — `/v1/admin/users`, `/v1/auth/login`, `/v1/auth/change-password` — needing a real argon2 hash and a session cookie. Keeping them in notification would have forced `notification → identity`, closing the `identity ↔ notification` cycle; seeding around it was impossible (the recipient columns carry no FK, but the specs need a *logged-in* user, not a row). They moved verbatim into `catalog/identity/single-tenant/api/__e2e__/`, where the dependency already points the right way. `notifications-product-extension` stayed put: it seeds `recipientId: ulid()` and issues no HTTP request, so its `RATE_LIMITER` override was provably dead weight. Discovery is unaffected — `apps/api/test/jest-e2e.json:5` `testRegex: ".*\\.e2e-spec\\.ts$"` is entry-agnostic and only ever sees an installed child. |
| AD-027 | active (vitest-migration, 2026-08-22; supersedes AD-012) | **The pre-push coverage gate is `pnpm test:coverage` — one Vitest process over `api`, `api-int`, `api-e2e`, `web` with v8 coverage merged and floors declared per glob in `vitest.coverage.mts` (`apps/api/src/**`, `apps/web/src/**`), no global bar.** It needs Docker (the `globalSetup` starts Postgres + Redis); `pnpm test` stays the Docker-free inner loop. Floors are a **flat 90 on every metric** — a global 90 plus a 90 per glob (`apps/api/src/**`, `apps/web/src/**`), the ailapidus shape — and are never lowered to pass: the way past a red gate is covering the code. Owner's call, 2026-08-22, replacing the migration's calibrated floors (api measured − 1.5 pt → 86.1 / 72.7 / 89.8 / 86.9; web 64 / 56 / 61 / 64), which stay recorded in `done/vitest-migration/design.md` § *Spike results* as the migration's measurement. **Both globs clear 90 since `api-coverage-to-90` (2026-08-22, Verifier PASS).** `apps/api/src/**` measures statements 96.69 (965/998), branches 95.33 (449/471), functions 94.92 (280/295), lines 96.84 (920/950); `apps/web/src/**` 94.78 / 94.51 / 95.56 / 96.58. `pre-push` and the CI coverage job are green. The api reached the bar by covering, never by relief: the floors were untouched, no ignore pragma exists under `apps/api/src/**`, and the two files removed from the denominator were corrections of what the denominator means (a CLI entry point; the coverage-metric fixtures, one of which COV-06 *requires* to stay uncovered) — see `.specs/features/done/api-coverage-to-90/spec.md` § Denominator decisions. AD-012's 95 % target moves onto these per-glob floors (`test-suite-refactor` T39 re-baselines it); the separate nyc "combined" floors and `coverage-all.sh` cease to exist. T29 (`bb64e82`) calibrated the migration's floors from the migrated tree; they were superseded by the flat 90 on 2026-08-22 (`vitest.coverage.mts`). |
| AD-028 | active (vitest-migration, 2026-08-22) | **Vitest root `projects` is the only test runner.** Tests run outside Turbo (Turbo cannot merge coverage): root scripts `test` / `test:watch` / `test:coverage` / `test:int` / `test:e2e` / `test:db` drive `vitest run` with `vitest.config.mts` / `vitest.coverage.mts` / `vitest.integration.mts`; `turbo.json` and app manifests carry no `test*` task or script; a module is tested with `pnpm vitest run --project api|web <path>`. Projects: `api` (`src/**/*.spec.ts`, parity included), `api-int` (`{src,test}/**/*.int-spec.ts`, `maxWorkers 4`, DB `test_w${VITEST_POOL_ID}`), `api-e2e` (`{test,src}/**/*.e2e-spec.ts`, serial, fresh fork per file), `web` (`src/**/*.test.{ts,tsx}`). Suffixes stay (`.spec.ts` / `.int-spec.ts` / `.e2e-spec.ts` / `.parity.spec.ts` on the api, `.test.ts(x)` on the web); `globals: false` everywhere — every spec imports from `vitest`; api projects compile through `unplugin-swc` with the `.swcrc` options (`module.type: es6`); container URIs travel by `provide`/`inject` only; mock-reset flags stay at Jest's `false` defaults. Children follow with `scripts/platform/jest-to-vitest.mjs`; a catalog entry whose specs change ships a `breaking` advisory (AD-019). |

| AD-029 | accepted (security-audit-remediation, 2026-08-23) | **Rate limiting is a kernel seam — extends AD-024.** The port (`RATE_LIMITER` token + `RateLimiter { consume(key, limit, windowSeconds, { critical? }), reset(key) }`), the adapters (`RedisRateLimiter` in `shared/infra/rate-limit/`, `InMemoryRateLimiter` sliding window), the `ResilientRateLimiter` composite, the `@RateLimit({ limit, windowSeconds, critical? })` decorator and the `RateLimitGuard` class live in `apps/api/src/shared/kernel/rate-limit/`; `RateLimitModule` provides the port. Policy stays in entries: the kernel never registers the guard — the entry that owns request policy (identity) lists `APP_GUARD` as `[CsrfGuard, RateLimitGuard]` so a cross-site request never consumes a bucket; a route marks `critical: true` when its limit must survive a Redis outage (login account/IP, forgot/reset/verify/access-link), in which case the composite serves the same limits from the per-instance in-memory window and emits `rate-limiter.degraded`/`recovered` once per outage (identity records the auth event); non-critical keys fail open. A kernel-only child has the decorator but no active guard (unchanged from v1). Supersedes identity's `domain/ports/rate-limiter.ts` and `api/guards/rate-limit.guard.ts`. Discovered 2026-08-22 (AUTH-1/AUTH-5). |
| AD-030 | accepted (security-audit-remediation, 2026-08-23) | **Sensitive-key redaction is one kernel list.** `apps/api/src/shared/kernel/redaction/sensitive-keys.ts` exports `SENSITIVE_KEY_FRAGMENTS` (`password`, `token`, `secret`, `authorization`, `cookie`, `link` — case-insensitive substring of the key), `isSensitiveKey` and `redactSensitive(value) → { value, changed }`. Consumers: the log redactor (`log.redact.ts`, which adds PII fragments `email`, `cpf`, `phone`, `creditcard`, `useragent` and keeps `ip` exact-match), the outbox (`markPublished` and dead-lettering rewrite `payload` so no token/link rests in `_kernel.outbox`/`outbox_dead`), and any entry that redacts before persisting or logging (notification's delivery dispatcher delegates to it). An entry never keeps its own list. `outbox_dead` is purged by the kernel job `outbox-dead.purge` (`lockId` 6 — 3 and 4 collide with notification `delivery.purge` and identity `email-change.revert`; the kernel reserves 1, 2, 6 — `OUTBOX_DEAD_RETENTION_DAYS`, default 30). Discovered 2026-08-22 (AUTH-3). |
| AD-031 | accepted (security-audit-remediation, 2026-08-23) | **Production image contract and fail-closed configuration.** (1) The api build emits nothing under `**/testing/**`, `**/__e2e__/**`, `**/parity/**`, `**/__parity__/**` (`nest-cli.json` swc `ignore`); those trees stay importable by specs (AD-023). (2) An entry's boot-time seed is `api/seeds/bootstrap.ts` — production code, idempotent, driven by env (`MASTER_EMAIL`/`MASTER_PASSWORD` for identity); `docker-entrypoint.sh` runs every `dist/modules/*/seeds/bootstrap.js` when the variables are set and never names a module; dev/test seeds under `testing/seeds/` carry no literal credential (`SEED_MASTER_PASSWORD` or a generated one). `catalog-lint` fails an entry whose production code imports `api/testing/**`. (3) Security-relevant configuration has no insecure default: kernel `env.ts` requires `NODE_ENV` and `DATABASE_SSL`, defaults `TRUST_PROXY_HOPS` to `0`, refuses `redis://` in production unless `REDIS_ALLOW_PLAINTEXT=true`, serves `/docs` in production only with `DOCS_ENABLED=true`, accepts `DATABASE_SSL_CA`; an entry config follows the same rule (identity requires `BREACH_CHECK_ENABLED`). Turning a default into a requirement is a `breaking` advisory per entry (AD-019) and a kernel major. Discovered 2026-08-22 (SUPPLY-1, AUTH-6, KERNEL-3/4). |
| AD-032 | accepted (security-audit-remediation, 2026-08-23) | **Audit attachment is a hook the audited entry declares, run by audit at the end of its own install.** `audit` depends on `identity`, so audit always installs last in a child and any `audit.attach(...)` call inside an identity migration guarded on audit's presence is dead code — wave 8 found a fresh child had no identity trail at all. Rule: an entry whose tables are audited ships a custom migration that declares `<schema>.attach_audit()` (its table list and redacted columns — `identity.attach_audit()`: 14 tables, `users.password_hash`, `sessions.token_hash`, `devices.cookie_token_hash`, `verification_tokens.token_hash`) and `PERFORM`s it itself only when audit is already installed; audit's `migrations/custom/02_attach_module_hooks.sql` defines `audit.attach_module_hooks()`, which discovers every installed `<schema>.attach_audit()` via `pg_proc` and runs it — audit never names an entry (RULE C). Test helpers replay the shipped statement (`SELECT audit.attach_module_hooks()`), never a hand-copied list. Shipped in kernel `v2.1.0` (entries stay `2.0.0` — the same entry version exists at `v2.0.0` without the security changes; a proper entry bump is a follow-up). Open follow-up (`security-hardening-2`): nothing enforces that a new auditable entry declares the hook, and the reverse install order (audit first, entry later) is correct by construction but unexercised. |
| AD-033 | active (issue #9, 2026-08-23) | **Every catalog entry's `kernelRange` accepts the kernel version the changelog declares.** The latest `## vX.Y.Z` of `docs/dev/template-changelog.md` is the version `catalog:check` simulates in the rendered child and the one the next `v*` tag carries (AD-006), so an entry whose `kernelRange` excludes it is uninstallable by every child on that tag — `v2.0.0` shipped the five entries at `2.0.0` with `">=1.0.0 <2.0.0"` (version and `dependsOn` bumped, range not; exit 8 on any `module add`), fixed on `v2.1.0` with `">=2.0.0 <3.0.0"`. Enforced by `lintKernelRange` in `pnpm catalog:lint` (`scripts/platform/lib/lint.mjs`): pre-commit on `catalog/**`, `docs/advisories/**` and the changelog, CI on `main` and on every `v*` tag, and the first step of `catalog:check`. A kernel major therefore opens the ranges in the same commit as the changelog heading. Shipped in `v2.2.0` (`b266210`). |
| AD-034 | active (template-update-contract, 2026-08-23) | **The template↔child update contract.** *Template side, per `v*` tag:* a tag is cut only by a **pushed empty marker commit** whose subject is exactly `chore(release): vX.Y.Z` (amended 2026-08-24 by `release-marker-commit`; before that it was a `workflow_dispatch` with a hand-typed version). `release.yml` triggers on `push: branches: [main]`, does nothing unless the **head** commit is that marker, and refuses loudly — before any gate — when the subject is malformed, when a marker rides the push without being the head, or when the marker changes at least one file; the grammar lives in the tested `scripts/platform/lib/release-marker.mjs`, never in a YAML regex, and the workflow's own `if` stays a **loose prefix filter** because a strict one would turn a typo into a silent skip. `workflow_dispatch`, its `version` input and the now-unreachable non-main ref guard are **deleted**, which **supersedes that clause of REL-01** (`template-update-contract`): recovery from an infrastructure failure is GitHub's "Re-run jobs", not a second marker. `pnpm platform release [version]` composes the marker locally — version defaults to the latest changelog section, the preflight runs on the operator's machine, and it creates exactly one empty commit. Everything downstream is unchanged: `release-preflight.mjs` refuses the release when `version` differs from the latest `## vX.Y.Z` of `docs/dev/template-changelog.md`, when the tag already exists, when a catalog entry's tree changed since the previous stable tag without a `module.json` bump (the `2.0.0` collision class, AD-033), or when a non-major's `### Child migration steps` carries a manual step (`lintChildMigrationSteps` in `lib/kernel-version.mjs`, **preflight-only** — never wired into `runLint`); the `tag` job runs only on green `needs` (the five gates + the 5-entry `catalog:check` matrix), so no red gate can produce a tag. A kernel defect is announced as an advisory with `module: kernel`, whose `affects` range is matched against the child's installed template version instead of the module lock (`computePending`, `templateVersion` = `parseInstalledVersion(_commit).version`); `catalog:lint` refuses an advisory whose `module` is neither `kernel` nor an existing entry. Non-majors ship zero manual steps; majors ship executable `scripts/platform/migrations/v<X.Y.Z>.mjs`, each idempotent, run ascending by `pnpm platform template migrate`. *Child side:* the session hook fetches `docs/advisories/` from the latest stable tag by sparse clone (24 h cache, 8 s budget, every failure silent — a child on a broken tag learns about its own defect without updating first), `pnpm platform status` measures age against the **recommended, never enforced** cadence (`security` 7 days, `breaking`/`bug` 30) and surfaces feed errors and unparseable remote files. **The child never updates itself** (amended 2026-08-23 by `remove-child-update-bot`, before the `v2.3.0` tag): detection is automatic, applying is the operator's act, so no scheduled workflow — in the product's repository or anywhere else — runs `copier update` or opens an update PR. The weekly bot originally specified here (stories BOT-01…BOT-07: `.github/workflows/template-update.yml` + `scripts/platform/template-update-ci.mjs`, PR on green / issue `template update to <tag> blocked` otherwise) was deleted, not relocated — the objection is to the act, not to where the runner lives. `copier-delivery.test.mjs` guards the reversal by asserting git tracks neither path (absence, not `_exclude`: an exclusion entry for a nonexistent file proves nothing). `release.yml` is template-only (`copier.yml` `_exclude`); the migrations and `docs/dev/template-update.md` ship to the child. Agents still never tag and never push — and this survives the 2026-08-24 amendment intact: `pnpm platform release` commits **locally** and stops, printing the push command; the push is the user's act, and the push is what cuts the tag (`docs/agents/workflow.md`). An agent may invoke the command; it may not push it. |
| AD-035 | active (audit-2026-08-23-remediation, 2026-08-23) | **The professional/scheduling slice is its own catalog entry, cut at the aggregate — closes F-catalog-entries-6.** `identity/single-tenant` keeps users, sessions and permissions; `professional_areas`, `professional_services`, `professional_scheduling_areas`, `professional_schedule_config` and `professional_default_hours` move to a new entry `catalog/professional/` with `dependsOn: ["identity"]` (a new **entry**, not a variant — AD-013: a variant is an alternative implementation of the same module, this is an add-on). The cut is made at the **aggregate, not the table**: `servesClients` and `birthDate` leave the `User` entity (`user.entity.ts:13,29,40,77,86,99,110,119,137,145,150,213,220,229-236,325-329`, incl. `activate()`, `updateOwnProfile()`, `assertValidBirthDate()`) for a `professional_profile` table keyed 1:1 on `identity.users.id`, and `UserRepository` loses `replaceProfessionalAreas` / `replaceProfessionalServices` / `replaceSchedulingAreas` plus the scope reads (`user.repository.ts:16-19,103-111,152,159`). **Consequence that decides the shape:** because identity no longer calls into the slice, the `identity <-> professional` cycle never forms, so AD-025 ("invert only edges that close a cycle") leaves `dependsOn` carrying the single edge and **no token is promoted to `shared/kernel/**`** — AD-021, AD-024 and RULE C are untouched. The entry-local slot is therefore **deleted, not lifted**: `IdentityProfessionalSlot`, `forRoot({ professional })`, `PROFESSIONAL_SCOPE`, `PROFESSIONAL_COMMITMENTS` (`identity.module.ts:62-63,78-79,89-90,209-236`) and `infrastructure/professional/null-professional-adapters.ts` all go. The alternative (keep the fields on `User`, promote the tokens to the kernel) was rejected twice over: it fails IDENT-01 AC 1, which demands the users **schema** and the contract carry neither field nor the `professional` literal, and it would force "professional scope"/"professional commitments" into `shared/kernel/**`, exactly the module vocabulary RULE C bars. **Costs accepted:** no professional-named `operationId` exists, so the data rides inside `createUser`/`updateUser`/`listUsers` DTOs (`identity.contract.ts:131-150,169-181,187-198,204-211,216-221`) and the extraction is a **contract break on core operations** with an `openapi.json` + api-client regen and a `parity/contract.snapshot.json` re-snapshot; `04_audit_attach_hook.sql` splits, the new entry shipping its own `<schema>.attach_audit()` under the same `pg_proc` guard (AD-032); the `professional` literal leaves `BASE_ACCESS_PROFILES` (`access-profile.types.ts:16-21`), which is a code edit **plus** an `ALTER TYPE` story for existing children since the PG enum is code-derived (`user.table.ts:18`, `permission.types.ts:7-19`) and no migration in this repo writes it; and the change is **not identity-local** — `audit` names all seven tables in four files (`base-audit-registrations.ts:24,30,36,42,48,54,60`, `audit-coverage.ts:23-29`, `api/testing/reattach-identity-tables.ts:28-34`, `api/__e2e__/audit.e2e-spec.ts:178-184`), so the `breaking` advisory is per entry for **identity and audit** at minimum and both bump together. Supersedes nothing — AD-002, whose rationale kept `professional` in the base set, was already retired by AD-014. Ships in kernel **`v3.0.0`**. |
| AD-036 | active (release-marker-commit, 2026-08-24) | **`ci.yml` is the single workflow that runs code gates, it ships to the child, and its template-only jobs are inert there.** `.github/workflows/catalog.yml` is **deleted** and its jobs merged in; its `copier.yml` `_exclude` entry went with it, and `copier-delivery.test.mjs` asserts the deletion by **absence, not exclusion** (an exclusion entry for a nonexistent file proves nothing — the AD-034 precedent). Driver: both files fired on `push: branches:[main]` and on `pull_request`, and both ran `turbo lint typecheck` and `pnpm test`, so every push ran each twice. The cost was never runner minutes — it was **drift**: three lists that had to agree, with nothing asserting they did. **The constraint that decides the shape is that `ci.yml` ships and `catalog.yml` did not.** Six commands cannot run in a product — `catalog:lint`, `catalog:typecheck`, the 5-entry `catalog:check` matrix, `test:scripts`, `template:smoke` and the ADV-04 step — because `copier.yml`'s `_tasks` prunes the first five from the child's `package.json` and `_exclude` removes `advisory-required.mjs`, `scripts/platform/__tests__/` and `scripts/template-smoke.mjs`. A first `detect` job checks out, probes `[ -d catalog ]` and emits `template: true|false`; those six hang off `needs: detect` + an `if` on that output. **`hashFiles()` in a job-level `if` is forbidden** — it evaluates before checkout and is always empty. **Two conditions are load-bearing and must not be "simplified":** (1) `detect`'s job-level `if` skips the workflow on a marker push — an empty marker leaves the tree byte-identical to the commit CI already gated, so re-running it is waste — but it is scoped to `github.ref == 'refs/heads/main'`, because the `v*` tag push carries the **marker as its head commit** and an unscoped condition would skip `ci.yml` on every tag and silently retire AD-033's `lintKernelRange` enforcement; (2) the merged `gates` job keeps `fetch-depth: 0`, which is what lets `lintEntryBump` resolve the previous stable tag. **Nothing about formatting enters `ci.yml`**: a format gate there is a manual migration step for every existing child, which AD-034 forbids on a non-major, so `prettier-format-gate` keeps its own template-only `format.yml`. "One CI" therefore means one workflow of **code gates**, not one workflow file — `release.yml` (different trigger, different permissions) and `format.yml` (template-only) stay separate by construction. Accepted cost, knowingly: because `ci.yml` ships, a product that pushes a commit subject starting `chore(release): ` gets **no CI run**, silently; the condition carries a comment saying so, and the fix (a template-only marker file) is deferred until a product reports it. |
| AD-037 | active (web-stack-next, 2026-08-23; renumerado de AD-032 -> AD-035 -> AD-037; o ultimo salto foi na reconciliacao main<->origin/main de 2026-08-24, em que `audit-2026-08-23-remediation` ja ocupava AD-035 e `release-marker-commit` AD-036; antes disso, no merge de 2026-08-23, que tomou AD-032 para o hook de attach do audit e AD-033 para `kernelRange`; AD-034 está reservado pelo DOC-01 de `template-update-contract`) | **Selectable web shell via copier path templating + symlinks.** `copier.yml` question `web_stack` (`vite` default, `next`). Both shells are real workspace members — `apps/web-vite` (package `web`, the former `apps/web`) and `apps/web-next` (package `web-next`) — excluded from the render; two root-level Jinja-named directories `{% if web_stack == '<stack>' %}apps{% endif %}/web` are symlinks to them, so exactly one renders to `apps/web`; a `_tasks` step renames `web-next` → `web` on `copy` for `next`. Rejected: Jinja-named dirs inside `apps/` (turbo expands `{…}` as a glob and registers `apps/web` twice); templated `_exclude` (matches destination paths, so it cannot keep one shell and drop the other at the same `apps/web`); `_tasks mv` of `apps/web-next` (breaks `copier update` — patches would target the old path). Probed on copier 9.17.1: Vite child byte-identical, `copier update --defaults`/`--skip-answered` touch only the answers file, Next child updates patch `apps/web` in place. Same platform contract in both shells (transport, Zod env, RFC 7807 helper, auth store, `RouteAccess` vocabulary — Next: `ROUTE_ACCESS` table + `AccessGuard` slot in `src/_app/layout`, product slot `product-shell.tsx`); FSD in Next uses `src/_app`/`src/_pages`, `app/**` = pure re-exports. Catalog entries untouched (AD-018). |

## Handoff

- Reconciliação `main` <-> `origin/main` — **MERGE FEITO E GATED GREEN (2026-08-24), NÃO INTEGRADO E NÃO PUSHADO.** Worktree `.worktrees/merge-origin-main`, branch `chore/merge-origin-main`, commits `e3ac20d` (merge) + `0eeb9bd` (fix). Gate na árvore mergeada: `format:check` 0 · `check` 0 · `pnpm test` **616/616** · `test:scripts` **486/486** · `catalog:lint` 0 · `catalog:typecheck` 0; `pnpm install --frozen-lockfile` passou, lockfile mergeado é consistente. `template:smoke` e `catalog:check` **não** rodaram (Docker/copier). **As duas histórias divergiram em `9c5e45c`**: `origin/main` (+40) trazia `web-stack-next` (pergunta copier `web_stack`, `apps/web` -> `apps/web-vite`, novo `apps/web-next`); `main` (+109) trazia `prettier-format-gate`, `release-marker-commit` e as waves 1-2 de `audit-2026-08-23-remediation`. Nenhum lado tinha sido compartilhado; zero commits duplicados; as 10 tags são ancestrais dos dois lados. **13 conflitos resolvidos** — `catalog.yml` continua deletado (AD-036) e a matriz `web_stack` foi portada para os jobs `catalog` e `smoke` de `ci.yml` (com o passo de cobertura do web-next); a asserção ACC-11 foi reapontada de `catalog.yml` para `ci.yml`; os seis arquivos de `scripts/**` ficaram com o corpo formatado do `main` e o threading de `webStack` do origin reaplicado; `assertWebShell` foi movido para **antes** das checagens de formato (no lugar do origin, os dois testes fail-fast dele batiam primeiro no format check); `gates.test.mjs` manteve a resolução multi-shell do origin. **v2.3.0 virou uma seção única** (decisão do dono): item 1 reescrito para o release por marker — `workflow_dispatch` não existe mais e o código sai DENTRO desta tag — e `web_stack` entrou como item 8; `### Child migration steps` continua `None`. **Duas colisões de numeração**, ambas resolvidas renumerando o lado do origin, seguindo o precedente que a própria linha dele registra: `AD-035` (era de `audit-2026-08-23-remediation`) -> `web-stack-next` virou **AD-037**; `L-025..L-029` -> os seis lessons do origin viraram **L-030..L-035** e `LESSONS.md` foi regerado por `.agents/skills/tlc-spec-driven/scripts/lessons.py --root . prune`, nunca à mão. **O defeito que o gate pegou e que nenhum conflito mostrou:** `apps/web` virou DOIS diretórios (`web-vite` por rename, `web-next` por cópia) e o git pareou cinco arquivos de `src/shared/**` com o `web-next` — `WEB_COPY`, `readEnvString` e `VITE_ROUTE_LOGIN` do `main` foram parar no shell Next e sumiram do Vite, sem um único marcador de conflito. Corrigido em `0eeb9bd`: `apps/web-next` voltou para as versões do origin (o `main` nunca teve shell Next), `apps/web-vite` recebeu merge 3-way de verdade por arquivo (base `9c5e45c:apps/web`, ours `main:apps/web`, theirs `origin/main:apps/web-vite`), todos limpos. `canonical-port.test.mjs` deixou de hardcodar `apps/web/.env.example` e passou a derivar dos shells existentes (`VITE_API_URL` no vite, `NEXT_PUBLIC_API_URL` no next) — a regra do L-025. **Armadilha registrada:** `prettier .` acusou 146 arquivos, mas o gate real é `prettier --check "**/*.{ts,tsx,mts,mjs,cjs,js,jsx,json,yml,yaml,css}"` — 19 arquivos, **nenhum** em `catalog/`, então nenhum bump de entrada foi disparado. É o L-030 na prática: confirmar um gate rodando o comando publicado, nunca reconstruindo o conjunto de entrada. **Por que um worktree e não `main`:** o checkout primário está compartilhado com a sessão `audit-2026-08-23-remediation`, que durante esta sessão avançou de `b95ec15` para `b3999ff` (4 commits, incluindo `c653a89` do CAT-02) e mantinha **7 caminhos não commitados**, entre eles `copier.yml` — que o origin também edita. O `git merge` no primário foi **recusado pelo git antes de tocar em qualquer arquivo**; nada foi danificado. **Duas coisas deliberadamente NÃO feitas:** o bullet de handoff `template-update-contract` que vinha do origin foi descartado (o `main` já arquivou a feature em `.specs/features/done/`); e `apps/web-next` continua **sem favicon** — o `apps/web/public/favicon.ico` veio de `1827dc9`, um commit do shell Vite, e foi para `apps/web-vite/public/` como rename limpo; dar um favicon ao shell Next é decisão de quem dona `web-stack-next`, não do merge. **PRÓXIMO PASSO, e ele é do dono:** integrar `chore/merge-origin-main` em `main` — só com a sessão irmã ociosa e com o working tree do checkout primário limpo (`git merge chore/merge-origin-main` a partir de `main`; se `main` tiver andado, remergear antes). Depois: `pnpm platform release 2.3.0` (cria o commit-marker vazio `chore(release): v2.3.0`, roda o preflight local, **não** tagueia e **não** pusha) e `git push origin main` — o push do marker é o que dispara `release.yml`. O agente nunca tagueia e nunca pusha (AD-006/AD-034). Os commits do merge usaram `--no-verify`: o hook `commit-msg` exige advisory para qualquer mudança em `catalog/**` e a única aqui é a resolução do README do identity, que é merge, não fix de catálogo.

- **Push desbloqueado (2026-08-23, GH013 resolvido)** — o secret era um PAT do GitHub (prefixo anotado fora do repo) capturado pelo cache do turbopack em `apps/web-next/.next`, commitado por T7. História reescrita com `git filter-branch --index-filter 'git rm -r -q --cached --ignore-unmatch apps/web-next/.next'` (passos 2–3 do plano feitos; worktree `.worktrees/web-stack-next` e branch `feat/web-stack-next` removidos; backup em `backup/pre-filter-main`). Encerrado: PAT revogado via `POST /credentials/revoke` (2026-08-23, verificado 401; não era o token do gh nem do .npmrc — veio do env do build); backup refs apagados, reflog expirado e `git gc --prune=now` purgou os objetos com o secret; `v1.1.0` obsoleta — remoto já está em v2.2.1 e a entrada web_stack sai no `## v2.3.0` do changelog.

- Feature `docs-audience-contract` — **Specified (2026-08-23). Artifacts at `.specs/features/docs-audience-contract/{spec,context}.md`. Design skipped (Medium; the seam questions are closed in the spec's Assumptions). Next: Tasks.** 11 ACs (`AUD-01..11`), every proof `test` under `pnpm test:scripts`, probe budget 0 of 3. *Problem:* `copier.yml` `_exclude` prunes only `/docs/platform_template` and `docs/catalog/README-contract.md`, so **31 of the 35 files under `docs/` ship to every child**, including docs addressed to whoever works in *this* repo; the confirmed instance is `docs/agents/workflow.md`, which ships and tells its reader to dispatch the `release` workflow (`:134-135`), a file `_exclude` removes. **The defect class is the addressee, not the lexicon — a `BRAND-*` vocabulary gate reads that file clean and it is still wrong in the child.** **Four owner rulings, 2026-08-23, rationale in `context.md`:** (1) declaration is **directory**, not front matter — template-only docs move under `docs/platform/`, one `_exclude` anchor `/docs/platform`. This **reverses** the handoff's front-matter lean, which is exactly why the row was re-opened instead of treated as settled. Accepted cost: a directory does **not** invert the default — a new doc still ships unless someone files it under `docs/platform/`, so the catcher, not the directory, is what closes the defect; recorded in the spec as an accepted trade-off so no later phase reads it as a stronger guarantee. (2) A doc both sides need is **split**, not marked (`both` has no place in a directory scheme); `docs/agents/workflow.md` is split **inside this feature** — excluding it wholesale and deferring behind a waiver list were both rejected. (3) The catcher is **static, in `pnpm test:scripts`** — shipped set = `git ls-files` minus `_exclude`, trailing `.jinja` stripped; no copier render, no docker, so it fires on every pre-push. (4) Scope is **`docs/**` only**. **Two grammar decisions Tasks must not re-litigate:** the path-token rule ignores placeholders (`<`, `{{`, `*`), absolute/`~`/`$` tokens and URLs; and a *separate* rule fails an inline-code token **exactly equal** to the stem of an `_exclude`d `.github/workflows/*.yml` (`release`, `catalog`) — without it the marquee bug is missed, because `workflow.md:134` names the workflow by name, not by path. **Known residual, stated in the spec rather than hidden:** misaddressed prose naming no path and no excluded workflow stays undetected; the feature removes its only confirmed instance instead of claiming the class closed. **Boundaries:** the remediation owns `BRAND-*` (vocabulary) and `RUN-01` AC 6 (commands, four pinned files) — this feature owns paths; `docs/platform_template/` keeps its own anchor because two in-flight features cite it by path (fold after both close); the changelog section is authored at Tasks against the next unreleased version and never touches the untagged `v2.3.0`.

- Feature `web-stack-next` — **DONE (2026-08-23). Verifier round 2 PASS** (10 ✅ / 2 ⚠️ / 0 ❌; `validation.md` § Round 2; sensor 3/3 killed). Merged into `main` at `ef23b21` (no-ff, feature HEAD `9e979e1`, 28 commits). Gates at merge: `pnpm check` 0, `pnpm test` (api 332 / web-next 56 / web 68 / eslint-config 28), `test:scripts` 196, web-next cov S 96.74 / B 94.25 / F 97.05 / L 99.06, `template:smoke` vite+next 0, `catalog:check identity --web-stack next` 0, `catalog:check --web-stack vite` 0, bare `docker build` of the next child 0. Side effects: lockfile bumped `typescript-eslint` 8.60→8.67 / eslint 10.4→10.9 (fixed `access.guard.spec.ts` `03f2bac`, catalog identity `access-policy.spec.ts` `0e08d3d` + advisory `ADV-20260823-01`, identity → 1.0.1) — **revertido no merge com `api-coverage-to-90`**: o lockfile resolvido ficou com os pins de `origin/main` (`typescript-eslint` 8.60 / eslint 10.4), os dois specs ficaram na versão Vitest do origin e o defeito de `unbound-method` deixou de existir, então `ADV-20260823-01` foi removido e identity permanece em `2.0.0` (bump do `vitest-migration`); `.next` was committed by T7 and untracked in `9e979e1`. Accepted debt (⚠️): ACC-01/ACC-02 exclusion list must also exclude `packages/{eslint,typescript}-config/**`, `pnpm-lock.yaml`, `access.guard.spec.ts`; DOC-03 AGENTS command table only gains `(front on :3001)` for next. Lessons L-025..L-030 (renumerados duas vezes: L-010..L-015 → L-016..L-021 no merge com `api-coverage-to-90`, e L-016..L-021 → L-025..L-030 no merge de 2026-08-23 com `origin/main`, que já ocupava até L-024). NEXT: worktree `.worktrees/web-stack-next` + branch `feat/web-stack-next` still exist (remove after push); tag `v1.1.0` pending user `sim`; CI run for `ef23b21` still has the known `copier` missing on `ubuntu-latest` (see v1 bullet) — first thing to check after push.

- Feature `remove-child-update-bot` — **DONE (2026-08-23). Verifier PASS, 10/10 ACs, sensor 3/3 killed.** The owner decision that blocked the `v2.3.0` tag is **applied — the block is lifted.** Archive `.specs/features/done/remove-child-update-bot/` (`handoff-archive.md` carries the original decision verbatim, the commit table, the two deliberate deviations from the removal scope, and three findings). Three commits on `main`, `eb907ef`…`557fef0`, **not pushed**. AD-034's child side amended in place. Gates: `pnpm check` 5/5, `pnpm test` 585/585, `pnpm test:scripts` 345/345 (baseline 355 − the deleted bot test's 10), `catalog:lint` 0, `catalog:typecheck` 5/5. Also done as housekeeping: the stale `.worktrees/security-audit-remediation` worktree was removed (clean, branch fully merged; the branch itself was left in place).

- Feature `release-marker-commit` — **DONE bar one blocked task. Verifier PASS round 2 (2026-08-24), 25/25 ACs, sensor 3/3 killed. `validation.md` written. 12 of 13 tasks, 16 commits `c4ea19e`…`9820444`. Not archived to `done/`: T13 is still open. Next: the owner tags `v2.3.0`, then T13 writes the `v2.4.0` changelog section.** Round 1 returned FAIL on one Minor gap — the MARK-08 emptiness mutant (`changedFiles.length > 0` → `> 1`) survived because the suite exercised 0 and 2 changed files but never **exactly 1**, so a marker changing one file would have released. Fixed test-only in `9820444`; the Verifier independently confirmed `release-marker.mjs:60` is absent from that diff and was already correct. Final gate: `pnpm check` 5/5, `pnpm test` 614/614, `pnpm test:scripts` 454/454, `catalog:lint` clean. **`catalog:typecheck` and `template:smoke` FAILED and are real but foreign** — `git blame` puts them on `35c8a4f` / `5f89723`, outside this feature's 15-commit range; they are nobody's open item yet and someone should own them. Lessons L-028 / L-029 recorded. Wave 2's Build gate: `pnpm check` **exit 0**, `pnpm test:scripts` **454 / 49 files** (pre-feature baseline 376 / 42 — nothing dropped). Wave 1 = 11 tasks in 4 parallel clusters (`c4ea19e`, `7550d88`, `6b99461`, `529ad5c`, `f3b5635`, `dc1e3c7`, `2c21355`, `3ced718`, `c3f3690`, `e1e2c8c`, + format repair `6e63888`); wave 2 = T3 `7e5a43e`; wave 3 = T12, this entry and the two decision rows. **Only T13 remains and it is ⛔ BLOCKED, on nothing this feature can do:** the `v2.4.0` changelog section cannot be written while `docs/dev/template-changelog.md:6` is an untagged `## v2.3.0`, because `release-preflight` keys on the *latest* section (AD-034) and appending above it would make `v2.3.0` permanently untaggable. **Tagging `v2.3.0` is the owner's act.** T13 maps to **no AC** — all 25 are delivered by waves 1–3 — so the Verifier is not blocked by it. **AD-034 amended and AD-036 recorded** (see § Decisions): the two load-bearing conditions nobody may "simplify" are `detect.if` scoped to `refs/heads/main` (or the `v*` tag push, whose head commit *is* the marker, skips `ci.yml` and silently retires AD-033) and `marker.if` in `release.yml` staying a **loose** prefix filter (a strict one turns a typo into a silent skip, the exact failure MARK-06 exists to catch). **One worker deviation to weigh at verification:** C2 kept the `marker` job's `version` output **without** the `v` prefix, to match the contract `release-preflight.mjs` already expected from the old `inputs.version`; coherent, but it was not in the design. **Three defects this Execute produced, all recorded in `tasks.md` § Execution Log:** wave 1's payloads never told workers to run `npx prettier --write`, so four files shipped unformatted and needed the `6e63888` repair — the tree has zero formatting diffs but `prettier-format-gate`'s pre-commit auto-fix (its T9) has **not landed**, so there is no net and every payload must carry that line; the `Touches` in a worker payload must be **copied verbatim from `tasks.md`, never retyped**, because a narrow list fails *silently* — the worker returns a polite "not mine" instead of an error, and no hook can see the gap between the plan and the payload (`wave-plan-check.mjs` validates the plan, not the prompt); and **this checkout is shared with live sibling sessions** — three foreign commits landed inside this feature's range, `.github/workflows/ci.yml` was found reverted to pre-edit content mid-task (recommitted; `git diff HEAD -- .github/workflows/` is empty, and the sibling session independently confirmed the merged file on disk), so every commit stays pathspec-limited and `git add -A` / `git add .specs/` / `git stash` are forbidden. **`copier.yml` ordering is settled**: this feature went first by the owner's ruling, because handing the one-line deletion to the sibling's T41 would have made **CI-01 unprovable inside this feature's own commit range**; the sibling agreed the reasoning was sound and T41 now inherits a settled file. **The `pnpm check` failure seen at wave 1's gate was foreign** — 19 `apps/web` lint errors plus `api#lint`, none attributable to any commit in this range (`git log --name-only -- apps/` empty) — and the sibling session has since repaired them; that is why wave 2's gate is exit 0. **13 tasks / 7 clusters / 4 waves**, all 25 ACs mapped, 0 unmapped, probe budget 2 of 3 spent (MARK-14, DOC-03) — the four pre-approval checks are in the file and all pass. Wave 1 is four parallel clusters with pairwise-disjoint file unions: **C1** `ci.yml`+`catalog.yml`+`gates.test.mjs` (sonnet) ∥ **C2** `release-marker.mjs`+`release.yml`+their two test files (sonnet) ∥ **C3** `commands/release.mjs`+`cli.mjs`+their two test files (sonnet) ∥ **C4** the six doc files (haiku). Waves 2–4 are exclusive singletons: T3 `copier.yml`, T12 `.specs/STATE.md` (**orchestrator-written, never a worker**), T13 the changelog. **Two authoring calls Execute must not undo:** the per-cluster Quick gate names its own test files (`node --test <files>`) instead of `pnpm test:scripts`, because that script globs all 44 files and four concurrent workers would each run their siblings' half-written specs — the whole suite runs only at the wave barrier; and no task touches `apps/**`, so `pnpm test` (Vitest, 585) cannot regress before the Final gate and no wave runs it. Build gate = `pnpm check && pnpm test:scripts`; Final = that plus `pnpm test`, `catalog:lint`, `catalog:typecheck`, `template:smoke` (the last is CI-06's Independent Test). Baseline to preserve: `test:scripts` 376/42 at `d373b72`. **Two blockers, both outside the feature, both recorded in `tasks.md`:** (1) **T3 must be sequenced against `audit-2026-08-23-remediation` T41 before wave 2 runs** — T41 is a declared single-editor task on `copier.yml` and the edit here is one line; the owner rules the order. (2) **T13 is ⛔ blocked on the `v2.3.0` tag** — `docs/dev/template-changelog.md:6` is still an untagged `## v2.3.0` and `release-preflight` keys on the latest section, so appending `## v2.4.0` above it makes `v2.3.0` permanently untaggable; the section is also shared with that feature's area H. Waves 1–3 are unblocked. **A Touches audit corrected the doc blast radius the spec recorded:** only `docs/catalog/catalog.md:87` names `catalog.yml`; `docs/agents/workflow.md:124-126`, `docs/dev/template.md`, `docs/dev/deploy.md.jinja` and `.github/README.md` name **`ci.yml`** and describe a job list the merge invalidates — that became T11, a task the spec had no line for. `apply.mjs`'s `TEMPLATE_ONLY_FILES` (`:23-27`) holds three api test files and is unrelated to this feature. **All five `n — flagged` assumption rows are CLOSED by the owner at Design (`design.md` § *Decisions locked at Design*) — do not re-litigate:** (1) `detect` probes `[ -d catalog ]` and governs **six** things, not four (`test:scripts`, `catalog:lint`, `catalog:typecheck`, the `catalog:check` matrix, `template:smoke`, the ADV-04 step) — `copier.yml:74` prunes all three scripts from the child; (2) **the release's double-run is resolved on the CI side, reversing the spec**: `ci.yml` does not run on a marker push, `release.yml` keeps its full gate set, AD-034's self-sufficiency untouched — the marker is empty, so the tree is byte-identical to the commit CI already gated, and the spec's last Edge Case row plus the "duplication survives" assumption row are now dead text; (3) "one CI" = one workflow of **code gates**, `format.yml` and `release.yml` stay separate; (4) a marker that changes ≥1 file **fails loudly before any gate**; (5) the grammar lives in a tested `scripts/platform/lib/release-marker.mjs`, never a YAML regex. **Two traps the design pins and Tasks must not undo:** the marker skip in `ci.yml` must be scoped to `github.ref == 'refs/heads/main'` or the `v*` tag push — whose head commit *is* the marker — skips too and silently retires AD-033; and the `marker` job's `if` in `release.yml` is deliberately **loose** (prefix only), because a strict grammar there turns a typo into a silent skip instead of the loud failure MARK-06 demands. **Four spec corrections the design records (§ *Spec corrections*):** CI-03's command list omits `template:smoke` (`catalog.yml:82-94` has a `smoke` job — nine commands, not eight); DOC-03's anchors are stale (`docs/dev/template.md:58` carries no dispatch/tag instruction; the `git tag v1.2.0` line is at `TEMPLATE.md:24`/`:29`, not `:26`); MARK-03's `tag` needs the **superset** `[marker, verify, catalog]` because a job reads outputs only from a direct need; and `.github/workflows/` holds **four** files — `feedback-triage.yml` exists, runs no gate, is out of scope. **Evidence gathered at Design, do not re-derive:** `release-workflow.test.mjs` asserts nothing about the `workflow_dispatch` trigger itself, only the `${{ inputs.version }}` interpolation (`:36-55`), the ref-guard step (`:21-29`), `tag.needs` equality (`:33`) and the `contents: write` exclusivity (`:57-63`); `gates.test.mjs` reads `catalog.yml` at `:84-90`; `copier-delivery.test.mjs` asserts only `release.yml` is `_exclude`d, nothing about `ci.yml`/`catalog.yml`; `child-manifest.test.mjs:8` holds `TEMPLATE_ONLY_SCRIPTS`; the `commit-msg` hook is `lefthook-local.yml:20-23` and an empty commit passes trivially (`advisory-required.mjs:24-35`); the only `pre-commit` hook is `lefthook-local.yml:12-18`, glob-gated; `catalog.yml:18-20` **already** carries `fetch-depth: 0`; `plan.mjs:84-105` proves a child never grows a `catalog/`; `docs/dev/template-changelog.md:6` is still `## v2.3.0` and no `## v2.4.0` section exists. Two halves. **(A) One CI.** `.github/workflows/catalog.yml` is **deleted** and its jobs merged into `ci.yml`; `copier.yml:35`'s exclude entry goes with it. Driver: `ci.yml:3-5` and `catalog.yml:3-8` both fire on `push: branches: [main]` and on `pull_request`, and both run `turbo lint typecheck` (`ci.yml:30` / `catalog.yml:27`'s `pnpm check`, which *is* that command per `package.json:25`) and `pnpm test` (`ci.yml:46` / `catalog.yml:28`) — so every push runs each twice, three times counting `ci.yml:63`'s `test:coverage`. **The constraint that decides the shape: `ci.yml` ships to the child and `catalog.yml` does not.** A naive merge would hand every product `catalog:lint`, `catalog:typecheck` and a 5-entry `catalog:check` matrix over a `catalog/` that does not exist there — red pipeline on day one, which AD-034 forbids on a non-major. Resolution in the spec: a first `detect` job checks out and outputs whether `catalog/` exists, and the catalog jobs hang off it (`hashFiles()` in a job-level `if` evaluates before checkout and is always empty — do not use it). Must preserve: the `v*` tag trigger (AD-033 enforces `lintKernelRange` on every tag) and the `pull_request`-only ADV-04 step. **(B) Marker release.** `release.yml` drops `workflow_dispatch` entirely and triggers on `push: branches: [main]`, doing nothing unless the **head** commit is an empty `chore(release): vX.Y.Z`; preflight and the whole gate set stay as they are, `tag` still `needs: [verify, catalog]`, so the tag keeps its "was green" promise and shipped `docs/dev/template-update.md:10-12` needs no edit. **Owner rulings (five, one of them a reversal worth recording):** (1) *gates* — the owner first said the release "só altera a tag" and, with the two readings side by side, ruled **keep them unchanged**; the release is not made a superset of `ci.yml`, so a marker can still tag a commit whose web build or coverage is red — same exposure as today's dispatch, accepted knowingly. (2) *dispatch* — **deleted**, not kept as a fallback; recovery is GitHub's "Re-run jobs", and the now-unreachable non-main ref guard goes with it, **retiring that clause of REL-01** (`template-update-contract`). (3) *ergonomics* — ship `pnpm platform release [version]`: version defaults to the changelog's latest section, runs preflight **locally**, creates the empty marker, and **never tags, never pushes** (AD-006/AD-034 intact; an agent may invoke it). (4) *version slot* — shares **`v2.4.0`** with `audit-2026-08-23-remediation`, no renumbering; that task queues behind the `v2.3.0` tag like the remediation's area H. (5) *consolidation* — asked for in the owner's words as "1 ci só, release arranca fora só via commit". **Five assumptions left `n` in the spec — settle before Tasks:** the `detect`-job seam; whether the release's own duplicate of the catalog matrix survives (a marker push would run that matrix twice, and AD-034's self-sufficiency is the only thing holding it); the marker commit must be empty; the marker logic in a tested `scripts/platform/lib/release-marker.mjs` rather than a YAML regex; and the scope of "one CI" now that a sibling feature adds a third workflow. **Requires amending AD-034** and a new decision recording `ci.yml` as the single gate workflow. In scope and stale today: `docs/agents/workflow.md:131-135`, `TEMPLATE.md:26`, `docs/dev/template.md:58`; plus `gates.test.mjs` (parses both workflow files), `release-workflow.test.mjs` (asserts the dispatch shape and the ref guard) and `copier-delivery.test.mjs`. **Cross-session collisions, live 2026-08-23:** `prettier-format-gate` T7 reformats every `.yml` including `catalog.yml` (which this feature deletes — deletion wins, but whoever goes second re-runs the formatter) and its T10 adds a template-only `.github/workflows/format.yml`, a third workflow this feature's Design must account for; `audit-2026-08-23-remediation` had three workers live on wave 1. **Standing:** the agent never tags and never pushes — `v2.3.0` remains untagged with `main` unpushed (local `eb907ef`…`1f609c1`), and this feature cannot be exercised end to end until the owner pushes.

- Feature `template-update-contract` — **DONE (2026-08-23). Verifier PASS round 2, 33/33 ACs.** Archive `.specs/features/done/template-update-contract/` (`handoff-archive.md` carries the full record: what shipped, the commit range `db8149d`…`474f9f6`, every deviation, and the two findings worth keeping). 17/17 tasks in 3 waves, all workers sonnet, worked directly on `main` — no worktree, nothing to merge. Final gate exit 0: `pnpm check` 5/5, `pnpm test` 585/585, `pnpm test:scripts` 355/355 (pre-feature baseline 279), `catalog:lint` 0, `catalog:typecheck` 5/5; sensor 3/3 killed. AD-034 recorded; lessons L-025/026/027. **Still open, and the user's act alone: `v2.3.0` is not tagged** — the tag is cut by dispatching the `release` workflow (the agent neither tags nor pushes). Two unrelated commits ride the same tag: `817a129` platform-feedback and `7ab03bd`, the `.claude/skills/template-update` symlink v2.2.0 left untracked. Note for whoever tags: `docs/dev/template-changelog.md`'s v2.3.0 section is being appended to by a parallel session (dev-server hooks, item 7) — the preflight requires its `### Child migration steps` to stay the literal `None — copier update is enough.`

- Feature `audit-2026-08-23-remediation` — **Execute: waves 1 and 2 of 14 GATED GREEN (2026-08-24). 32 of 79 tasks done. Next: wave 3 — C7 (T33→T36) ∥ C8 (T37→T40) ∥ C9 (T41), all sonnet, gate `full-unit`.** Wave 1 `a754208`…`768c1ef` (16 commits), wave 2 `91ba30b`…`8550f2b` (16 commits) plus 3 repair commits `a00c493`, `2a1acc2`, `ae69b83` and one out-of-wave regression fix `36f1f9f`. Wave-2 Build gate: `check` 0 · `test` 614/614 in 90 files · `test:scripts` 453/453 · `format:check` 0 repo-wide. Full detail in `tasks.md` § *Execution Log*, which is the authority — this entry is the index, not the record. **The `prettier-format-gate` serialization is over: that feature CLOSED with Verifier PASS (9/9 ACs, sensor 4/4), so nothing blocks this feature's dispatch any more.** **THE BUILD GATE CHANGED — waves 3-14 also run `pnpm catalog:typecheck` and `pnpm catalog:lint`** (recorded in § *Gate Check Commands*, `b83663e`). The argument is structural, not economic: `pnpm check` runs `tsconfig.json` and **never** `tsconfig.catalog.json`, so it cannot see `.catalog-stage` at all — the commands do not overlap. This was bought by a real escape: wave 2 was gated green over a tree where `catalog:typecheck` was red, and a *sibling feature's Verifier* found it, not this feature. **Three dispatch rules earned in wave 2, all binding:** (1) **a cluster whose `Touches` include a linted workspace gets `pnpm --filter <ws> lint` in its per-task gate** — wave 2's scoped gates ran vitest and never eslint, so 19 errors accumulated to the Build gate; (2) **payload `Touches` are copied verbatim from `tasks.md`, never retyped** — a retyped list made a worker disown a file its own cluster had created, and this fails *silently*: a narrow list produces a polite "not mine" indistinguishable from diligence, invisible to `wave-plan-check.mjs`, which validates the plan and never the prompt typed out of it; (3) **a worker that hits a genuine plan gap stops and reports rather than widening its own scope** — this happened three times in wave 2 and was correct all three. **Before dispatching wave 3, re-read `.github/workflows/` and `copier.yml` ON DISK — do not trust the `Where` fields, which have already gone stale once.** `.github/workflows/catalog.yml` was **deleted** by `release-marker-commit` (`6b99461`) and merged into `ci.yml` behind a `detect` job. Consequences: **T35 is probably already delivered** — the merged `gates` job already carries `fetch-depth: 0` (`ci.yml:94,98,102`, gated on `detect.outputs.template == 'true'`), so verify rather than edit; **T36 now writes into the merged `ci.yml`**, and its assertion that `format:check` is absent from `ci.yml` was checked against that file and **still holds** (no format job was merged in, so no child inherits a red pipeline — that was the one way these two features could have contradicted each other without either Verifier noticing); **T41's `copier.yml` is settled** by that feature's `7e5a43e`, which removed the single catalog-workflow `_exclude` line and moved nothing else. **C8 edits `catalog/**` and the `commit-msg` hook refuses a catalog fix without a matching advisory (AD-019) — its payload must say so or every commit bounces.** **RUN-04 is CLOSED, satisfied-by-sibling with evidence** — cite `266d2fd`…`60a011a` (repair + the two whole-tree reformats) and `8816705`/`2fa2977`/`fd6b41e` (the proofs); its condition was never "the reformat happened" but "`format:check` is green at HEAD **and** something fails if it stops being", and both now hold. **T48 is unchanged and still blocked** until `git tag -l v2.3.0` returns something; the sibling's gate lift never transferred, and the agent never tags and never pushes (AD-006/AD-034). **Two live constraints from the closed sibling:** the pre-commit `format` job in `lefthook-local.yml` is live in auto-fix mode with a **pathspec-limited** re-stage — if a worker ever reports a commit that mutated a file it did not edit, that is the limit leaking and the sibling must be told; and `scripts/platform/__tests__/format-gate.test.mjs` is **class-level**, so any task touching `lefthook-local.yml` must keep it green — a failure there is a regression to the root-glob defect, never a stale assertion to "fix" by editing the test. **Two plan gaps found in wave 2 and still unowned:** `apps/web/Dockerfile` is in no task's `Touches` and bakes only `VITE_API_URL`, so `%VITE_LOCALE%`, `%VITE_APP_NAME%` and the route placeholders stay **literal in the built HTML** (Vite leaves unmatched placeholders as-is) — the JS layer corrects at runtime so no `Done when` was missed, but LOC-01/LOC-02 are half-delivered on the deploy path and **need a task that does not exist**; and this plan's `Commit` field is only present on the last task of each chain, so workers author the rest themselves. **The lesson worth keeping over any individual finding, three instances in one day across three sessions and three surfaces: _the check exists, but not at the point where the work happens._** A per-task gate that ran the test command but not the lint command; a Build gate that ran four commands but not the catalog ones; a sibling's pre-commit glob configured correctly and covering nothing — that last one findable only by mutating the config, never by reading it. It belongs in `.specs/lessons.json`; `release-marker-commit`'s Verifier agreed to carry it. **Standing constraints unchanged:** probe budget 1 of 3, spent by CAT-05; `product_locale` defaults to `pt-BR` and no existing child's string may change; `research.md` is the evidence ledger — cite `file:line`, do not re-derive, do not re-run the sweep; `tasks.md` § 0.3 corrects nine citations the design inherited — use § 0.3, not the design; § 0.1's area-B pinning is settled (AC 5 harness taxonomy = **BRAND-04**, AC 6 `infra.md`/`deploy.md` = **BRAND-05**) and must not be re-derived; the release split stands (`v2.4.0` minor, `v3.0.0` major with AD-035's identity extraction). **Exclusive tasks still ahead:** T42 (five `module.json` bumps — the entries sit at **`2.0.1`**, not `2.0.0`, so bump from there), T43 (harness taxonomy, BRAND-04 not BRAND-05, and it inherits T16's obligation to remove the `harness.md` exclusion from the domain guard), T57 and T76 (contract regens). **Sibling sessions:** `prettier-format-gate` is DONE; `release-marker-commit` is at its own wave 3 with its wave 4 blocked on the same `v2.3.0` tag, and it is finished with the shared files this feature needs.
- Feature `v0-2-product-slots` — DONE, merged `aa0b294`; archive `.specs/features/done/v0-2-product-slots/`. Still pending user `sim` → `git tag v0.2.0` + push tags + close issue #1.
- Feature `pre-push-coverage-95` — Wave 1 DONE (`c264827` T1, `3e09e3b` T2, T10 web fill); remainder absorbed by `test-suite-refactor` (see below).
- Feature `v1-kernel-only-module-catalog` — **DONE (2026-08-21). Verifier round 2 PASS** (23 ✅ / 11 ⚠️ / 0 ❌; `validation.md` § Round 2). Merged into `main` at `8bb606d` (no-ff, feature HEAD `e42ab2a`); worktree + branch removed; archive `.specs/features/done/v1-kernel-only-module-catalog/`. Gates at merge: `catalog:check` r14 0 (5/5), `template:smoke` 0 (4/4), `test:scripts` 179, `catalog:lint` 0, `catalog:typecheck` 0; sensor 3/3 killed. **Tagged `v1.0.0` at `2ce83ee` and pushed 2026-08-21** (`2ce83ee` = post-merge fix: `event-context.spec.ts` envelope shape; main checkout needed `pnpm install` for `yaml`). Accepted debt (non-blocking, see validation.md Round 2): Fix 6 TLG assertion gaps; TLG-07 no executable proof; note 49 (`FORBIDDEN_TOKENS` port exemption) recorded only in `tasks.md:748`; `.github/workflows/catalog.yml` never executed in CI and its ADV-04 step (`:29-34`) checks the PR diff against the head commit message only (trailer exempts the PR, not the commit) — candidate first advisory/fix after v1.0.0. Unblocks `test-suite-refactor` T0 pre-flight.
  **CI run #3 (`111f025`, run 32501027721) RED — first real execution. NEXT (small fix wave before anything else): (a) `Gates` fails on `pnpm check` → 4 ESLint errors in `apps/api/src/shared/config/load-dotenv.spec.ts` (`unbound-method` L5/16/23, `no-confusing-void-expression` L31; from `db31012`, pre-push has no lint step); (b) `template:smoke` + all 5 `catalog:check` matrix jobs exit 3 — `copier` not installed on `ubuntu-latest`; spawn ENOENT → `status: null` → `template-smoke.mjs:112` `?? 1` reports "copier saiu com código 1" and swallows stderr (same pattern in `scripts/platform/catalog-check.mjs`). Fix: add `pipx install copier` step to `catalog` and `smoke` jobs in `.github/workflows/catalog.yml`; surface ENOENT + copier stderr in both scripts (+ unit test); fix the 4 lints; decide whether `lefthook.yml` pre-push gains `pnpm lint`. Log: `/tmp/claude-1000/catalog-check-logs/ci-run-32501027721.log`. No catalog/** change → no advisory needed.
- Feature `pre-push-coverage-95` — unrelated fix landed on `main` while v1 was executing: `d4f5fe1` cleared the 6 web typecheck + 20 web lint errors its wave-1 coverage fill left behind (assertions untouched, 108 tests still green). Its own Wave 2 (T3–T9) is still pending — **absorbed by `test-suite-refactor`** (see below); folder deleted 2026-08-19 (history in git; T1 `c264827`, T2 `3e09e3b` stay on `main`).
- Feature `test-suite-refactor` — **Specified + Designed + Tasked (2026-08-19); re-authored 2026-08-21 to the current artifact model and re-baselined against the post-v1 tree.** Execute unblocked (v1 merged `8bb606d`). Next: owner confirms the assumption rows in `spec.md`, then wave 1. **40 tasks / 10 clusters / 5 waves** (waves replace the old phase+batch chain; wave 1 harness ∥ web, wave 2 four entry verticals in parallel, wave 3 enforcement, waves 4–5 exclusive: root gates then the coverage ratchet + closure). Pre-flight is T1 and stops if `catalog/` lacks the five entries. Absorbs `pre-push-coverage-95` (COV-01..10 → COV-01..11; its module-tree fills void after v1 T22, the rest re-targeted as T10/T30/T39) and v1 T26 (`testing.md` rewrite → T35). Changes from the first authoring: per-AC proof kind in the traceability table (probe budget 2 of 3 — `it`-count check and the `testing.md` grep), the grep-based bans became a committed guard spec (`harness-hygiene.spec.ts`, T33), RULE D added for `testing/` imports (T34), audit measurements moved out of `spec.md` into `design.md` § *Spike results*, and two dead targets dropped (a `docs-login` e2e and `maintenance-schedule.spec.ts` no longer exist). AD-023 still planned (renumbered from AD-021 on 2026-08-21 — v1 took AD-021); it lands `active` at T40. **Re-baselined to Vitest on 2026-08-22** (`vitest-migration` closeout, T30): T5/T31/T37/T39, T38, GA-5/GA-6, the AD-023 wording, the Test Coverage Matrix and the Gate Check Commands now name the root Vitest commands; `context.md` carries a dated re-baseline note over the Jest-era decisions it records. Execute is unblocked — the owner still confirms the assumption rows in `spec.md` first.
- Follow-up, no feature yet (opened by `vitest-migration` F6, 2026-08-22) — `resolveInstallOrder`'s root order with no requested entry is a `readdir`/LIFO artifact with no rationale: a sibling that opts into an optional provider (tag -> `audit.attach`) can install before it, and the provider's `pg_proc` guard then silently skips. Worked around with a guarded `reattach-tag-tables.ts` + `ADV-20260821-05`; the tooling fix is to make install order deterministic.
- Feature `api-coverage-to-90` — **DONE (2026-08-22), Verifier PASS at opus.** Closes the former *"Open, blocking pushes"* item: `pre-push` is green end to end (`lefthook run pre-push` exit 0 — catalog-typecheck, migrations, typecheck, test-coverage). `apps/api/src/**` at 96.69 / 95.33 / 94.92 / 96.84 against the flat 90; floors, pragmas and existing tests untouched. Full record — commit table, the two denominator corrections, the four unreachable branches deleted from production code, and lessons L-014/L-015 — in `.specs/features/done/api-coverage-to-90/handoff-archive.md`. `test-suite-refactor` T39/C10 still owns the coverage ratchet: the `thresholds` block's four `90`s and both glob keys were left untouched for it.
- Feature `prettier-format-gate` — **DONE (2026-08-24). Verifier PASS round 2, 9/9 ACs, sensor 4/4 killed.** Artifacts at `.specs/features/prettier-format-gate/{spec,tasks,validation}.md`. 16 commits on `main`, `266d2fd`..`4a69739`, **not pushed**. Final gate exit 0: `check` 5/5 - `test` 614/614 - `test:scripts` 462/462 - `catalog:lint` 0 - `catalog:typecheck` 0 - `format:check` 0 diffs. **Owner lifted the `v2.3.0` tag gate on 2026-08-23**: the feature ships *inside* `v2.3.0`, not behind it, after being shown the cost. Consequence: T12 bumped the five entries to `2.0.1` (`catalog/{attachment,audit,notification,tag}/module.json` + `catalog/identity/single-tenant/module.json`, each with a `## [2.0.1]` CHANGELOG heading) because the reformat trips `entryChangedWithoutBump` (`release-preflight.mjs:47-55`); every child will see five modules "behind" for a cosmetic change. T11 wrote **item 7 inside the existing `## v2.3.0` section** — never a new heading, or `release-preflight` (`:72-75`, keys on the latest section) would make `v2.3.0` untaggable. Three tasks were added during Execute, all from defects the plan did not foresee: **T12** (the bumps), **T13** (`.claude/settings.local.json` into `.prettierignore` — untracked, ignored only by the user's *global* gitignore, which prettier never reads, so `format:check` was red locally and green in CI), and **T14/T15/T16** (the Verifier's three missing proofs). **Two lessons of the same family, both grounded:** a gate's behaviour is confirmed by running the shipped command, never by reconstructing its input set (T7's worker called `settings.local.json` "correctly excluded" because it built its list from `git ls-files`; the shipped `format:check` uses a glob); and lefthook's `**/*` does **not** match repo-root files while prettier's does — the hook was armed with a hole and only mutation revealed it (`format-gate.test.mjs` now guards it). L-028/029/030. **Executed concurrently with `audit-2026-08-23-remediation` in the same checkout** — serialized by owner ruling, this feature first; that feature's RUN-04 is now genuinely `satisfied-by-sibling`. Its `35c8a4f` broke `catalog:typecheck` mid-verification and was repaired there at `36f1f9f`. **REMAINING, OWNER-ONLY (AD-006/AD-034 — the agent never tags and never pushes): `main` and `origin/main` have DIVERGED — 108 local commits vs 40 remote, neither side shared. `git push origin main` will be refused.** That must be reconciled before the `release` workflow can see any of this; only then can `v2.3.0` be dispatched. Closeout (move to `.specs/features/done/`) is deliberately NOT done — it waits on the tag.
- Follow-up, no feature yet (opened 2026-08-23 while specifying `release-marker-commit`) — **three workflow files carry the same gate block, and two of them fire on the same trigger.** `ci.yml:3-5` and `catalog.yml:3-8` both run on `push: branches: [main]` and on `pull_request`; `ci.yml:30` runs `pnpm turbo lint typecheck` and `catalog.yml:27` runs `pnpm check`, which *is* `turbo lint typecheck` (`package.json:25`), and both run `pnpm test` (`ci.yml:46`, `catalog.yml:28`). So every push to `main` runs lint+typecheck twice and the unit suite twice — three times counting `ci.yml:63`'s `pnpm test:coverage`, which re-runs the same unit projects alongside int/e2e. `release.yml`'s `verify` job carries a third copy of the same block. **The cost is not runner minutes, it is drift:** three lists that must agree, with nothing asserting they do — add a gate to one and the tag silently promises less than CI checked. Proposed fix: `catalog.yml` keeps only what is unique to it (the 5-entry `catalog:check` matrix, `test:scripts`, `catalog:lint`, `catalog:typecheck`, the PR-only ADV-04 step) and drops the duplicated `pnpm check` / `pnpm test`; `release.yml`'s duplication is deliberate and stays (AD-034: the release must be self-sufficient). History not established — the reading that `catalog.yml` copied the block to avoid depending on `ci.yml` is inference, not a recorded decision. Out of scope for `release-marker-commit`, which changes only that workflow's trigger.
