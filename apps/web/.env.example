# URL base da API (sem sufixo /v1 — os paths gerados já o incluem).
VITE_API_URL=http://localhost:3000

# Nome do app (título da página, pageTitle()) e idioma (<html lang>). Sem estas
# variáveis o app usa "Platform" / "pt-BR" — o comportamento de hoje.
VITE_APP_NAME=Platform
VITE_LOCALE=pt-BR
