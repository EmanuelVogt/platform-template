# Copie para .env e ajuste. O boot valida via Zod (fail-fast em variável faltando/inválida).
# Só as variáveis do kernel entram aqui. Cada entrada do catálogo declara as
# suas no `module.json` e o `pnpm platform module add` as acrescenta ao .env.
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug
# UI do Scalar em /docs. Em produção fica off a menos que ligada explicitamente.
DOCS_ENABLED=true

# Postgres dev existente (container postgres_dev). O kernel isola tudo no
# schema _kernel, sem colidir com o resto do devdb.
DATABASE_URL=postgres://devuser:devpassword@localhost:5432/devdb
DATABASE_POOL_MAX=10
# disable em dev; require em prod (TLS com rejectUnauthorized).
DATABASE_SSL=disable
# CA confiável para DATABASE_SSL=require (PEM; opcional, aceita \n escapado).
DATABASE_SSL_CA=
# Timeouts do pool (ms): conexão, ocioso e teto por query.
DATABASE_CONNECTION_TIMEOUT_MS=10000
DATABASE_IDLE_TIMEOUT_MS=10000
DATABASE_STATEMENT_TIMEOUT_MS=30000
# Rede de segurança abaixo do código: transação aberta e ociosa além deste teto é
# encerrada pelo Postgres (statement_timeout não cobre esse estado). Mesmo valor
# do statement timeout.
DATABASE_IDLE_IN_TX_TIMEOUT_MS=30000
# Teto da fila do pool: aquisições em espera acima disso são recusadas na hora,
# em vez de enfileirar. Default 20 = 2x DATABASE_POOL_MAX.
DATABASE_POOL_MAX_WAITING=20
# Acima deste tempo de espera por conexão sai o log db.pool_acquire_slow.
DATABASE_POOL_ACQUIRE_WARN_MS=500

OTEL_SERVICE_NAME=api
SERVICE_VERSION=0.0.1
# Vazio = SDK ativo sem exporter OTLP. Spans alimentam logs via traceId, sem backend visual.
# Dev usa ConsoleSpanExporter automaticamente.
OTEL_EXPORTER_OTLP_ENDPOINT=

# Dias de retenção de linhas mortas na outbox antes da purga (outbox-dead.purge).
OUTBOX_DEAD_RETENTION_DAYS=30

# --- rede / proxy ---
# Origin exata do front (sem default). CORS espelha só ela.
WEB_ORIGIN=http://localhost:5173
# Hops de proxy confiáveis até o app (atrás de LB ajuste ao número real).
TRUST_PROXY_HOPS=1

# --- redis ---
# Backend único de rate-limit e de cache do kernel. Obrigatório.
REDIS_URL=redis://:redis@localhost:6379
# true só quando REDIS_URL usa redis:// (texto plano) em produção.
REDIS_ALLOW_PLAINTEXT=false

# --- fuso da agregação por dia/semana (bucket-sql.ts) ---
# IANA validado no boot; ausente = UTC. Migrando de um filho `2.x` que
# recortava em outro fuso, declare-o aqui para preservar o corte de hoje
# (ex.: America/Sao_Paulo) — ver docs/advisories/ADV-20260824-04.md.
APP_TIMEZONE=UTC

# --- storage (S3-compat genérico; bucket único do kernel) ---
# Sem nenhuma destas vars, o boot sobe normalmente e a 1ª chamada de storage
# falha com StorageUnavailable — útil pra um produto kernel-only sem provider.
STORAGE_ACCESS_KEY_ID=changeme
STORAGE_SECRET_ACCESS_KEY=changeme
STORAGE_BUCKET=platform-dev
STORAGE_ENDPOINT=https://changeme.s3.example.com
# Sem default: R2 usa "auto", AWS S3 usa a região real do bucket (ex.: us-east-1).
STORAGE_REGION=changeme

# --- cookies (escape hatch pra migrar de um filho que já customizava os nomes) ---
# Sem estas vars, o kernel usa __Host-app_session, __Host-app_device e
# app_csrf. Um filho que precisa preservar os nomes que já tinha em produção
# por um deploy sem derrubar sessão declara os valores antigos aqui — ver
# docs/advisories/ADV-20260824-03.md.
# COOKIE_NAME=
# DEVICE_COOKIE_NAME=
# CSRF_COOKIE_NAME=
