# Fixture — waiver grammar

Uma entrada vive em `catalog/`, fora do copier, então o filho nunca
recebe. <!-- audience-contract: catalog/ — nomeado para explicar que não é entregue -->

<!-- audience-contract: catalog/schema/module.schema.json — comentário em linha própria não isenta nada -->

O schema em `catalog/schema/module.schema.json` valida o manifesto.

O registro fica em `catalog/registry.json`. <!-- audience-contract: catalog/registry.json -->
