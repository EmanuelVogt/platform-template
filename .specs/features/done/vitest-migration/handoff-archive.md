# Handoff archive — vitest-migration

Moved out of `.specs/STATE.md` § Handoff at closeout (T30, 2026-08-22). The feature is done:
merged into `main` with `--no-ff` at `bc63070`, no tag and no push (the owner's call).
AD-027 and AD-028 are `active`; AD-012 is superseded by AD-027.

- Feature `vitest-migration` — **Execute DONE. Verifier PASS (round 2, 2026-08-22): 24/24 ACs, Final gate exit 0 in a single run (9/9 steps, `catalog:check` 68 files/463 and `template:smoke` 4/4 green), sensor 3/3 killed; `validation.md` written.** Only **T30 closeout** remains. Worktree `.worktrees/vitest-migration`, branch `feat/vitest-migration` from `main` `e167341`; 30 tasks over 7 waves, every commit and gate in `tasks.md` § *Execution log*, every departure from the plan in § *Deviations* (1–27). Late waves: T28 `f431564` (jest/@swc/jest/@types/jest/nyc out of `apps/api`, `@vitest/coverage-v8` out of `apps/web`, lockfile regenerated), T29 `bb64e82` (**api floors 86.1 / 72.7 / 89.8 / 86.9** from measured 87.69/74.21/91.3/88.43 — istanbul's `percent()` truncates, which is why the floor is 86.1 and not the 86.2 the T18 reading implied, Deviation 25; web untouched at 64/56/61/64), then three round-1 fixes: F7 `4f6e376` (`ADV-20260821-03` now tells identity 1.x products to run the `ALTER COLUMN created_at SET DEFAULT clock_timestamp()`), probe scoping `00351a1` (Deviation 26), F6 `348606f` (`tag.tags` had no `audit_row` trigger in the **combined** 5-entry child: `resolveInstallOrder` with no requested entry is a LIFO `readdir` stack, so tag's attach migration runs before `audit.attach` exists and its `pg_proc` guard skips — fixed via the identity precedent with a guarded `reattach-tag-tables.ts`, not by retro-attaching from audit, which both entries' READMEs and SQL explicitly refuse; `ADV-20260821-05` documents the manual `SELECT audit.attach('tag','tags',…)` for products that installed tag before audit — Deviation 27). **T30 (orchestrator, no worker):** `merge --no-ff` into `main`, **no tag, no push** (owner's call). Expect rename/content conflicts in docs: main moved on at `653d757` (docs translated to English, arch docs now `docs/arch/{back,front}.md`) while C7 edited `docs/test/testing.md`, `docs/back/back-arch.md`, `docs/catalog/catalog.md`, `docs/agents/harness.md`, `AGENTS.md.jinja` in pt-BR on the branch — resolve by re-expressing the branch's content in English on main's files (main's language wins, the branch's content wins); the `docs/back/back-arch.md` edits belong in `docs/arch/back.md`. Then flip **AD-027** and **AD-028** from `planned` to `active` (AD-028 wording: `api-int` include is `{src,test}/**/*.int-spec.ts`; AD-027 records the calibrated floors above and that AD-012 is superseded), re-baseline `test-suite-refactor` (T5/T31/T37/T39, GA-5/GA-6, AD-023 wording, its Test Coverage Matrix and Gate Check Commands → the Vitest commands; drop the "blocked behind vitest-migration" note from its Handoff entry), move the folder to `.specs/features/done/vitest-migration/` and these entries to its `handoff-archive.md`. Open follow-up, outside this feature: `resolveInstallOrder`'s root order is a `readdir`/LIFO artifact with no rationale — a sibling that opts into an optional provider (tag → `audit.attach`) can install before it; a tooling fix would make install order deterministic.
