import { GetAccessCatalogController } from "./get-access-catalog.controller"

/** Catálogo de acesso (self-service — alimenta o builder de permissões do front). */
export const ACCESS_CATALOG_CONTROLLERS = [GetAccessCatalogController]
