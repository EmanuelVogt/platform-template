# Handoff archive — docs-audience-contract

Moved out of `.specs/STATE.md` § Handoff at closeout (2026-08-25). The feature is **done and
accepted**: 11/11 ACs evidenced, discrimination sensor 3/3 killed, and AUD-01/03/09/11 confirmed a
second time by direct inspection of an actually-rendered child.

The Verifier's recorded verdict is **FAIL**, and it stands as written — it is driven entirely by
foreign code. The Final gate's last unmet command, `pnpm template:smoke`, was **waived** because a
gate-by-gate reading of `scripts/template-smoke.mjs` shows none of its four gates reads `docs/`,
`copier.yml`'s `_exclude`, or the rendered child's doc tree — a green run would have added no
evidence for any AC. The full reasoning, including why waiting for the RULE D repair was rejected
(the fix already exists, verified, on branch `tsr-verify`), is in `validation.md`
§ *Closeout Decision*.

Nothing in the Decisions table was flipped by this closeout. AD-034 is untouched.

The bullet below is the `## Handoff` entry verbatim as it stood at `76eb379`.

- Feature `docs-audience-contract` — **EXECUTE COMPLETE 2026-08-25. All 12 tasks landed and each wave gated green; Verifier ran and returned 11/11 ACs matched with 0 spec-precision gaps and a clean discrimination sensor (3 mutants injected, 3 killed). Verdict is recorded FAIL for one reason only: the Final gate cannot go green on this checkout because `pnpm check` and `pnpm template:smoke` break on `apps/api/src/shared/infra/storage/null-storage.adapter.spec.ts` (5x TS2554), committed broken in `d5cfcaf` (`feat(api): provider-neutral STORAGE_* keys and a null storage adapter`) — a different feature, outside this one's diff surface (`docs/**`, `scripts/platform/__tests__/**`, `copier.yml`, `.claude/hooks/**`). `pnpm format:check` and `pnpm test:scripts` are green at 681/681 against a 630 pre-feature floor. Report at `.specs/features/docs-audience-contract/validation.md`. NEXT: once `apps/api` is repaired by its owner, resume the SAME Verifier with the fix range — no work in this feature is pending, and no fix task belongs to it. Both live sibling sessions (`platform-template-84`, `platform-template-0c`) were notified of the red `main`.** Commits: `254dbd4` plan · `aea599b` T1 · `aaa203d` T2 · `89c165f` T3 · `2f3ac8d` T4 · `68f01d1` regression repair · `6dd7c14` T5 · `44f53d6` T6 · `203f448` T7 · `df41664` T8 · `83cd9fc` T9 · `2629248` T10 · `79d17e9`+`2e0c31d` T11 · `95b02b0` T12 · `5d86d8b` T10 close-out · spec `755c300`, `5122386`, `f13a9d1`, `df49bc7`, `3a1d8ae`. Out of feature but dispatched here: `a2716e7` (`seam-no-edit` line-drift fix, duplicating `dcc10c8` from a parallel session — my error, I dispatched before checking whether the breaking commit's owner was already on it). **Four decisions taken during Execute that a later session must not silently re-derive:** (1) **The inline-waiver grammar is `<!-- audience-contract: <token> — <reason> -->` trailing the token's PARAGRAPH.** I pinned it per-LINE before wave 1 (the plan had left the spelling to T9, in wave 3, while T2/T3/T5/T6 in wave 1 already had to write waivers — three parallel workers would each have invented one). Per-line then broke on hard-wrapped prose and wave 3 widened it to the paragraph; an own-line comment still waives nothing, asserted both adjacent and separated. (2) **T12 targets a NEW `## v2.5.0` section, not `v2.4.1`.** The 2026-08-25 owner ruling sent it to `v2.4.1` because that was then the only untagged section; a parallel session pushed `3bef437 chore(release): v2.4.1` and cut the tag DURING wave 1, so every section is now released and appending would misreport what shipped. `v2.5.0` chosen mechanically (T7 is `feat`, child impact non-major with no manual step per AD-034) BY THE ORCHESTRATOR WITH THE OWNER ASLEEP under a standing instruction to finish without stopping — **reversible in one edit to `v2.4.2` if the owner disagrees; no other task depends on the number.** (3) **AUD-09 must never be asserted as the literal absence of `.worktrees/`.** T3 deliberately kept the `.worktrees/<slug>` convention and the `branch-only-in-worktree.mjs` lock on the child's side because `harness.md` and `AGENTS.md.jinja` cross-reference them and the child does use worktrees; only the shared-checkout rule, the no-PR policy, the `origin/main` anecdote and the `release` dispatch moved. T11 asserts those four, and the Verifier mutation-confirmed it fails if any returns. (4) **The feature created one instance of its own defect class and repaired it:** T3's move left `issue-tracker.md.jinja:62-63` citing a no-PR policy that had stopped shipping. C2 flagged it instead of widening scope; ruled in scope (the spec's accepted prose residual covers only PRE-EXISTING instances) and fixed in `68f01d1`. **Three accepted deviations, all with pinned blast radii:** the guard skips `.agents/skills/**` (19 of 22 live findings; synced third-party payload no task owns, and it returns the guard toward the owner's actual `docs/**` scope); AUD-04 exempts the changelog and `docs/platform_template/` as historical records; T10's new stem rule found a REAL live defect (`docs/dev/template-update.md:10`, `` `release` `` under "What the template promises per tag") that C5 pinned as a ratchet rather than exempt — the orchestrator closed it with a waiver in `5d86d8b` and shrank the assertion to expect ZERO live findings, because a guard shipping green while tolerating the defect it exists to catch is worse than a red one. **Lessons recorded by the Verifier:** `L-044` (verify only on a quiescent checkout — promoted to confirmed, recurrence 2) and `L-046` (assert an exemption's blast radius, not just its literal value). **Closeout is NOT done:** `.specs/features/docs-audience-contract/` stays out of `done/` until the Verifier returns PASS. **UPDATE (end of session):** the `apps/api` blocker is FIXED by its owner (`03e74f2`, session `platform-template-0c`, T54/wave 8 of `audit-2026-08-23-remediation`), so `pnpm check` and `pnpm template:smoke` should now pass on `main` — **the Final gate has NOT been re-measured since**. A Verifier resume was dispatched to run the Final gate in an isolated worktree (`.worktrees/verify-docs-audience-contract`) with the then-broken file neutralised locally; it had not returned when this session ended. **Simplest next step is to ignore that worktree run and re-run the Final gate on `main` directly, now that the blocker is gone** — `pnpm format:check && pnpm check && pnpm test && pnpm test:scripts && pnpm template:smoke` — then resume the same Verifier with the result so it can flip the verdict and finish `validation.md`. **`pnpm catalog:check` is separately RED on `main` and will stay red**: T49a bumped five catalog entries to 3.0.0 while inter-entry `dependsOn` stayed `>=2.0.0 <3.0.0`, which needs a semver-policy ruling from the owner and is not this feature's. **Two findings recorded in `tasks.md` § Execution Record that outlive this feature:** (a) **the module installer is a delivery channel with no audience contract** — `pnpm platform module add` vendors `catalog/*/README.md` into the child, but `/catalog` puts those files outside the copier shipped set, so the guard structurally cannot see them; measured instance `catalog/identity/single-tenant/README.md` § *Tests*, which sends a child reader to `catalog/<entry>/api/testing/**` when the code lands at `apps/api/src/modules/<entry>/testing/**`. Closing it means teaching `shippedSet()` about the installer — a feature, not a task. (b) **A second confirmed instance of the addressee defect class**, `5c0ad20`, which put "an agent may push `main` and cut release tags" into the shipped half of `workflow.md` inside a paragraph that opens "push = deploy" — invisible to both a vocabulary gate and this feature's own guard, since it names no path and no workflow stem. Repaired by its owner in `3acbe2f` using the `docs/platform/workflow.md` affordance T3 created. It is live evidence that the spec's declared residual is real: the prose-only half of the class produced a fresh instance within hours of the guard landing. **FINAL UPDATE — the Verifier returned and the previous 'just re-run the Final gate' advice above is SUPERSEDED.** It ran the Final gate in the isolated worktree and the verdict stays **FAIL**, driven by a **third** foreign defect: `apps/api/src/modules/module-boundaries.spec.ts` **RULE D fails inside the rendered child** (10 failures, only 1 surfaced — stderr preview truncated), which blocks `template:smoke` at gate 1/4. RULE D arrived in `ef9f9f8` (`test(catalog): RULE D — testing imports follow dependsOn`, the `test-suite-refactor` session), touches no `docs/**`, and its scope is unknown. **So re-running the Final gate on `main` will NOT go green** — the `apps/api` adapter fix (`03e74f2`) cleared only the first blocker. In the worktree everything else passed: `format:check` ✅ `check` ✅ `test` ✅ 735/735 `test:scripts` ✅ 681/0. **What IS now proven, and this was the point of the exercise:** the Verifier ran `pnpm template:smoke --keep` and inspected the preserved rendered child directly — `docs/platform/` is **absent**; `docs/` holds **33 files**, an exact match for `git ls-files docs/` minus `docs/platform*`; the four AUD-09 strings are **absent** from the rendered `workflow.md`; `docs/agents/README.md`'s table fully resolves. **AUD-01, AUD-03, AUD-09 and AUD-11 are therefore confirmed twice — by unit test and by the actual rendered artifact.** That is the faithful end-to-end proof the plan reserved `template:smoke` for, and this feature has it. The only thing missing is `template:smoke`'s full 4-gate chain running clean, which is gated on someone else's RULE D defect. **NEXT: this feature needs no further work. Either close it out on the evidence above (11/11 ACs, sensor 3/3, rendered-child confirmation) and record the `template:smoke` chain as blocked-by-foreign-defect, or wait for RULE D in the rendered child to be fixed by its owner and then resume the same Verifier.** That is an owner call, not a task. Report: `validation.md` § Isolated Worktree Measurement, committed `1fd8e7e`. The verification worktree `.worktrees/verify-docs-audience-contract` is disposable and was never merged; delete it freely.
